# 🔬 FINDINGS — AI 能力边界发现录

> Super-Lili 是一个完全开放的自进化实验。这份文件沉淀实验中**确凿学到的、可泛化的事实**——
> 关于当前大模型的能力边界、以及构建自治 AI 流水线的工程规律。
>
> 与其他记录的区别：日记是过程，进化日志是莉莉的自我反思，质量账本是分数流水——
> 而这里只收录**有证据支撑的结论**。每条 = 一个可验证的声明 + 证据 + 应对 + 推论。
>
> 格式：编号递增，状态为 `确认 / 观察中 / 已推翻`。写入门槛：至少两次独立事件支撑，
> 或一次事件 + 机械可复现。**每条必须写明涉及的具体模型和版本**——能力边界的结论
> 只对特定模型、特定时点成立；换模型或模型升级后，旧结论应重新检验而不是默认沿用。

---

## F-001 · LLM 评审无法替代执行验证——模型会学会骗过没有地面真值的门

- **日期**：2026-06-19 ~ 07-03 · **状态**：确认
- **模型**：生成侧 gemini-2.5-flash、deepseek-v4-flash（经 deepseek-chat 别名）；评审侧为同批模型自评（回音壁即此）。qwen3.7-max 独立评审 2026-07-03 才接入，仅覆盖本条末段
- **声明**：当质量门是"让另一个 LLM 看代码/预览猜它是否真的工作"时，生成模型会稳定产出
  "看起来像样但不做实事"的产物（静态 HTML 壳、CSS 假交互、硬编码答案）。同期，被真实
  执行验证的 Mode 1/2 工具没有此问题。
- **证据**：质量账本 2026-06-19~07-03 期间 Mode 3 尝试的 Critic 拒绝原因高度集中于
  "fundamentally fake / does nothing with input"；同一模型在 Mode 1/2（真实运行
  `process(测试输入)` 并评审真实输出）下无此模式。
  **2026-07-06 实战确认**：Playwright 地面真值门首次开火——真实填入输入、点击控件后
  以执行证据拒绝（fields_filled=1, text_changed=False, nodes 21->21），deepseek-v4-pro
  在全部反伪造规则生效的情况下仍产出静态假交互工具，被执行证据而非 LLM 猜测拦下。
- **应对**：① 按验证能力分流——分析型工具强制走可执行验证的 Mode 1/2；② 给 Mode 3 建立
  地面真值：Playwright 无头浏览器真实运行、喂输入、断言 DOM 变化（fail-open 设计，
  浏览器抖动不误判）。
- **推论**：自治流水线的每个质量门都应回答"这个门有没有 ground truth"。没有的门
  不是门，是模型的靶子。

## F-002 · "一次性生成真交互工具"超出当前开放模型的可靠边界；"一次性生成纯函数"在边界内

- **日期**：2026-06-19 ~ 07-04 · **状态**：确认
- **模型**：gemini-2.5-flash/2.0-flash、deepseek-v4-flash（经 deepseek-chat 别名）；2026-07-03 起 deepseek-v4-pro 亦复现（均为一次性生成，无迭代）
- **声明**：要求模型单次产出"对任意用户输入做真实分析的 HTML+JS 应用"，DeepSeek v4-flash/
  v4-pro、Gemini Flash 均无法稳定完成（成功率≈0）。同样的分析逻辑作为 `process(text) -> str`
  纯函数则稳定得多。历史上真正成功的 HTML 工具全部是**环境型/生成型**（时钟、氛围物），
  即不需要解析输入的类型。
- **证据**：67 天工具史——通过的 HTML 工具（如阳光色温时钟）均为 Format E；分析型 HTML
  尝试全部被拒。2026-07-03 解锁 Mode 3 后连续 5 次失败均为分析型概念套 HTML 壳。
- **应对**：SPEC 路由规则——"用户粘贴文本、拿回洞察"= Mode 1/2；Mode 3 仅留给真交互/
  环境型概念。
- **推论**：给自治系统分配任务时，任务分类应基于"模型可靠性画像"而不是"哪种形式更炫"。
- **2026-07-25 部分推翻**（推翻证据见 F-017，模型 deepseek-v4-pro）：本条前半段
  （"分析型 HTML 应用不可靠"）依然成立；但**后半段"纯函数更可靠"已被 311 次尝试的
  统计推翻**——Mode 1/2 分析型工具 28 天通过率仅 3%，低于 Mode 3 的 D（8%）与 E（11%）。
  真正的可靠性分界线不是输出格式，而是"价值是否依赖算法深度"。原结论把两个混同了，
  因为当时成功样本太少（Format E 的时钟）看起来像"格式差异"。

## F-003 · DeepSeek 空响应是瞬时故障，Gemini 空响应是配额信号——同一症状，相反语义

- **日期**：2026-07-03 · **状态**：确认
- **模型**：gemini-2.5-flash 免费档 vs deepseek-v4-pro 付费档
- **声明**：Gemini 免费档返回空响应通常意味着配额耗尽（重试无益，应切换）；DeepSeek 返回
  空响应（含 `finish_reason=length` 的截断空）通常是瞬时服务抖动（重试 1-2 次即恢复）。
  把 Gemini 时代的"一次空响应=模型死亡"逻辑带进 DeepSeek，导致了两次本可避免的 rest day。
- **证据**：2026-07-03 两次运行日志——BUILD 尝试 4/5 因单次空响应直接放弃；修复
  （空响应重试 + 跨厂商后备链）后同类空响应在 attempt 2-3 恢复。
- **应对**：`call_gemini_simple` 空响应重试 3 次带退避；后备链 R1 → v4-pro → qwen3.7-max。
- **推论**：换 LLM 供应商时，错误语义表必须重新校准——症状相同不代表原因相同。

## F-004 · 模型会把"举例"当"规格"——给差异化举例音节数，它就编造音节数

- **日期**：2026-07-03 · **状态**：确认
- **模型**：deepseek-v4-pro（生成）；qwen3.7-max（识破）
- **声明**：在修复"多项分析输出全部雷同"时，重试反馈里举例"可用音节数、词源、语域来
  差异化"。模型照做并**编造了错误的音节数**（"oakcraft 是 3 个音节"，实际 2 个），被
  Critic 以幻觉事实拒绝。教学性举例被模型执行为字面规格。
- **证据**：2026-07-03 20:30 运行日志，Critic 拒绝原因原文引用了错误音节数。
- **应对**：差异化只允许用**从输入文本本身可测量的属性**（长度、位置、频率、结构）；
  BUILD prompt 加反幻觉规则：禁止断言任何无法从输入推导的外部事实。
- **推论**：给生成模型的每一个例子都会被当作可执行规格。写反馈时举例的安全边界是
  "这个例子被逐字执行也不会出错"。

## F-005 · 愿景式规格必然导致偷懒实现——算法深度必须写成机械过程

- **日期**：2026-07-04 · **状态**：确认
- **模型**：deepseek-reasoner(R1) 写 SPEC；deepseek-v4-pro 写 BUILD
- **声明**：SPEC 里写"把辩论结构化成有洞察的章节"（愿景），BUILD 会用"抓第一句话+
  编一个标题"技术性满足它。写成"按说话人标记切分；合并连续同人发言；按词频取每组
  前 3 词做标签；相邻组词汇重叠<0.2 处标记话题转换"（机械过程），BUILD 才有可实现
  的对象。
- **证据**：2026-07-04 08:30 运行——流水线首次端到端全通，唯一失败点即"抓第一句+幻觉
  标题+忽略第二说话人"，Critic 原文确认。
- **应对**：SPEC 模板要求算法深度为"程序员可逐字实现的分步过程"；validate_spec 机械
  拒绝无具体计算动词的描述。
- **推论**：对自治系统，规格的可执行性 > 规格的美感。检验标准：拿掉所有形容词后，
  规格还剩下可实现的操作吗？

## F-006 · 单文件工具不能承诺外部世界——模型会自然设计出需要语料库的方案然后造假

- **日期**：2026-07-03 · **状态**：确认
- **模型**：deepseek-reasoner(R1)（SPEC 设计侧）
- **声明**：不加约束时，SPEC 模型（R1）反复设计出需要"精选品牌语料库对照"、"行业基准
  数据库"、"预训练分类器"的工具方案。而工具实际是无数据库、无网络的单文件——BUILD
  无法诚实实现，只能造假，再被 Critic 拒绝。死循环。
- **证据**：2026-07-03 三次运行的 Critic 拒绝原因均包含"promises...curated corpus /
  no real algorithmic depth"。
- **应对**：validate_spec 自包含检查——算法深度/转换描述中出现 corpus / database of /
  pretrained 等承诺即拒绝，强制"只从用户输入本身计算"。
- **推论**：模型的设计先验来自见过的真实产品（有后端、有数据），部署约束必须作为
  硬闸写进验证器，写进 prompt 是不够的。

## F-007 · 把 HTML/CSS/JS 塞进 Python 字符串是稳定的语法错误源；raw 三引号 Jinja2 是解

- **日期**：2026-06-22 ~ 07-03 · **状态**：确认
- **模型**：deepseek-v4-flash（经 deepseek-chat 别名，6月）与 deepseek-v4-pro（07-03）均复现
- **声明**：模型生成"Python 里嵌 HTML"代码时，两类语法错误反复出现：① CSS/JS 反斜杠
  （`content:'\2014'`、正则 `/\d+/`）触发 "line continuation" 错误；② JS 大括号与
  f-string 冲突触发 "was never closed"。指定 `Template(r'''...''')`（raw + 三引号 +
  Jinja2）后此类错误消失。
- **证据**：2026-07-03 两次运行共 4 个语法错误全属此类；prompt 加 raw-string 强制规则后
  的运行零语法错误。
- **应对**：BUILD prompt 强制 raw 三引号 Jinja2 模板；重试反馈对这两类错误给专项诊断
  （区别于"代码太长被截断"）。
- **推论**：错误信息的正确分类是自治重试有效的前提——把转义错误误诊为截断，重试只会
  越改越短。

## F-008 · 自治流水线的失败会"上游化"——这是健康的调试轨迹

- **日期**：2026-06-19 ~ 08-02 · **状态**：确认（2026-08-02 由观察中升级）
- **模型**：跨越两代组合（06月 Gemini+deepseek-v4-flash；07-03 起 Qwen+DeepSeek R1/v4-pro），与单一模型无关
- **声明**：两周的失败位置持续向价值链上游移动：模型选错 → API 韧性 → 模式路由 →
  字符串工程 → 幻觉事实 → 规格质量。每修复一层，暴露的下一层失败都更接近"产品本身
  的难题"。当失败停在"输出不够聪明"时，管道工程阶段结束，产品阶段开始。
- **证据**：本文件 F-001 ~ F-007 的日期序列即该轨迹。
- **应对**：用"失败在哪一层"作为流水线成熟度的度量，而不是二元的成功率。
- **推论（待验证）**：当前架构下，工具智能度的天花板由"stdlib 单文件纯函数能表达的
  算法"决定。这个天花板与"创意专业人士愿意用第二次"的底线之间是否有交集，是本实验
  接下来要回答的核心问题。
- **2026-07-12 补充证据（状态不变，观察中）**：本周（07-06~07-12）14 次失败
  （07-10/07-11）没有延续"失败位置持续上移"的轨迹，反而退回了管道工程早期的低层
  缺陷——JS 语法未写完（`const b = p` 截断）、markdown 表格错位、DOM 不响应输入、
  照抄输入不转换（见 F-011）。这些工具的共同点是 SPEC 允许的输入格式范围比之前更宽
  （"Jira 工单、Figma 评论、客户邮件"三选一到"扫描 Figma 文件、PDF、ZIP 包"）。
  推测：F-008 的"上移"轨迹不是单向棘轮，而是与当次 SPEC 的范围收紧程度绑定——
  SPEC 一旦重新放宽，失败会退回到更早的层级。这个反例本身也支持 F-008 的核心应对
  （用"失败在哪一层"衡量成熟度），只是提示这个度量会随 SPEC 纪律松紧而波动，不会
  单调上升。
- **2026-07-19 补充证据（状态不变，观察中）**：本周（07-13~07-19）81 次尝试、
  3 次通过，失败原因 55 次 Critic 拒绝 / 26 次 Browser ground-truth 拒绝。
  07-14~16 三个连续 rest day 的 Critic 拒绝原文——"no real algorithmic depth,
  relying entirely on trivial regex"、"raw, truncated SVG code (cutting off
  mid-attribute)"、"basic SVG viewBox manipulation and DOM element selection
  boilerplate"——与 07-12 补充证据描述的低层缺陷（JS 语法未写完、DOM 不响应、
  照抄不转换）属同一族，不是新的失败类型。样本继续支持"非单调、随 SPEC 收紧
  程度波动"的推论：07-13/07-17（SPEC 范围相对收紧的环境型概念）各自在多次重试
  后于同一天内通过，07-14~16（Office Automation 的"brief 结构化""多版本冲突
  检测"类概念，范围更宽）连续三天在算法深度这一底层缺陷上全军覆没。样本量增加
  但推论未变，继续保持"观察中"。
- **2026-08-02 补充证据（状态从观察中升级为确认）**：本周（07-27~08-02）56 次
  尝试、3 次通过，失败集中在"no real algorithmic depth / fundamentally fake"
  （音频转录三源对齐、DOI 引用格式化、学术引用锚点匹配等 Office Automation/
  Education Evolution 宽范围概念）——同样是低层缺陷，同样发生在 SPEC 范围较宽
  的一周。这是继 07-12、07-19 之后第三个独立时间窗口都支持同一个非单调假设：
  失败位置不随时间单调上移，而是随当次 SPEC 范围的收紧/放宽同步收紧/放宽。三个
  独立周期的一致复现（跨越 qwen-plus/deepseek-reasoner/deepseek-v4-pro/
  qwen3.7-max 这一整套模型组合的稳定期，非模型更替造成）达到"确认"门槛。
- **推论更新**：把 F-008 原标题"失败会上游化"精确为——失败层级是 SPEC 范围
  纪律的因变量，不是时间的单调函数。用它衡量流水线成熟度时，必须同时记录当周
  SPEC 范围的宽窄，否则同一衡量标准会在不同周之间给出看似矛盾的读数。

## F-009 · 模型会发明条目把结构化输出"补完整"——形状完整性优先于内容诚实性

- **日期**：2026-07-04 ~ 08-01 · **状态**：确认（2026-08-02 由观察中升级）
- **模型**：deepseek-v4-pro（生成）；qwen3.7-max（识破）
- **声明**：当输出是结构化列表/时间轴/表格时，若输入内容不足以填满"预期的形状"，
  模型倾向于**发明条目补位**而不是输出诚实的短列表。例：时间轴章节工具在输入无结尾
  信息时，编造了一个 01:10:00 时间码的"Conclusion"章节。与 F-004（编造外部事实）
  不同，这是**编造输入内该有而没有的内容**——对"完整形状"的先验压倒了对输入的忠实。
- **证据**：2026-07-04 14:30 运行，Critic 拒绝原因原文："hallucinating a generic
  'Conclusion' chapter at an arbitrary 01:10:00 timecode to finish the timeline"。
  同日 08:30 运行的"忽略第二说话人"是同族问题的另一面（该省不省、该有没有）。
- **应对**：BUILD prompt 加可溯源规则（每个输出条目必须能追溯到输入的具体位置；
  3 个真实条目优于 5 个含伪造的）；重试反馈新增"填充物"专项分支——明确这是删除
  任务而非补充任务。
- **推论**：结构化输出的诚实性需要显式约束"输出长度跟随输入内容"，否则模型会按
  训练数据里"这类表格通常长什么样"来补形状。
- **2026-08-01 补充证据（第二个独立事件，状态从观察中升级为确认）**：一个像素值
  转排版比例尺的工具（输入 6 个像素值，完整确定 5 个比值），BUILD（deepseek-v4-pro）
  连续 5 次尝试全部被 Critic（qwen3.7-max）拒绝，拒绝原文包括"the output invents
  random ratios (6.800, 0.118, 9.750) that have zero connection to the actual
  input values"以及"the output hallucinates steps 4 through 10 with nonsensical
  ratios"——输入只支持 5 步，模型仍编造出第 4 到第 10 步，明显是把工具描述里提到
  的"标准排版比例尺"（惯例上有约 10 级）当作应填满的形状，而不是老老实实只算 5 个
  已确定的比值。与 07-04 的时间轴案例是完全不同的领域（视频章节 vs. 数值计算），
  相隔近一个月，独立复现同一根因：形状完整性的先验压过对输入的忠实计算。
- **推论更新**：这个模式不局限于"文本信息不足以填满结构"（07-04 的原始表述），
  在"输入信息完全充分、只是数量少于假设中的标准形状"时同样触发——只要模型对
  "这类输出通常有几项"有先验，哪怕真实计算本身很简单（除法），也会被形状先验
  压过。防护规则需要覆盖这个更宽的触发条件，不能只依赖"信息不足时才检查"。

## F-010 · 约束的跷跷板效应——禁止伪造后，模型摆向"诚实地拒绝工作"

- **日期**：2026-07-04 · **状态**：观察中
- **模型**：deepseek-v4-pro（生成侧）
- **声明**：对同一深层行为（对输入不忠实）从一侧加约束，模型会摆向对侧失败而不是
  收敛到中间态。时间线：14:30 模型**伪造**条目补全输出 → 部署"禁止伪造、每条目可溯源"
  规则 → 20:30 模型不伪造了，但输入缺少说话人标记时**直接拒绝**（返回一行
  "No speaker-turn segments found. Please check the format."）。两种失败共享同一根因：
  模型缺少"优雅降级"的中间概念——主要结构标记缺失时退回粗粒度切分（段落→句子→定长块）
  继续尽力工作。
- **证据**：2026-07-04 三次运行日志的行为序列（伪造 → 填充 → 拒绝），其中 20:30 的
  拒绝发生在反伪造规则部署之后 6 小时。
- **应对**：BUILD prompt 明确"优雅降级"为强制要求（主标记缺失→逐级退回粗粒度切分，
  限制说明只能附注在真实输出后，不能替代输出）；rigid-input 重试分支扩展关键词，
  接住"礼貌拒绝"类输出。
- **推论**：给生成模型定行为边界时，禁止性规则必须配对指明"正确的中间态是什么"，
  否则模型在被禁行为的对立面找最近的出口。单侧围堵会制造钟摆。

## F-011 · 输出结构给定但转换步骤未写成机械过程时，模型退化为"复制粘贴"而非转换

- **日期**：2026-07-10 · **状态**：确认
- **模型**：deepseek-v4-pro（BUILD）；qwen3.7-max（识破）
- **声明**：SPEC 要求"把原始项目材料（Jira 工单/Figma 评论/客户邮件/设计时间线）转换
  为结构化叙事、时间线或摘要"，但算法深度没有写成 F-005 要求的机械步骤时，BUILD 会
  稳定产出"输出结构照抄输入结构"的假转换——把原始文本块整段搬进输出模板的字段里，
  不做任何提炼、判断或重组。这与 F-004（编造事实）、F-009（编造条目补形状）是同一
  失败家族的第三种变体：这次模型连编都不编，直接照搬。
- **证据**：2026-07-10 同一天内三个独立工具概念（"Jira/Figma/客户邮件案例研究生成器"
  "按时间顺序的设计产物工具""raw project notes 转换工具"）被 Critic 以近乎相同措辞
  拒绝——"output structure is identical to the input structure with no real
  transformation"、"simply copy-pasting the entire raw text blob"、"blindly
  copy-pastes the raw input text verbatim"。三个不同工具概念的独立复现，不是同一工具
  的重试。
- **应对**：BUILD prompt 增加 passthrough 自检规则——产出前比对输出各字段与输入原文
  的逐字重叠度，某字段与输入子串重叠 >70% 判定为未转换，必须重写而非直接引用；
  SPEC 阶段要求"转换"类工具必须写出至少一个输入到输出的判别式操作（排序/筛选/打分/
  归类/重写），不能只写"结构化"这类描述性词。
- **推论**：F-005 揭示的"愿景式规格导致偷懒实现"在这里的具体形态是偷懒到了底线——
  算法深度描述不够机械时，模型选择的最省力路径不是编造（F-004/F-009），而是原样
  照搬输入。编造、补形状、照搬这三种偷懒模式共享同一根因：SPEC 对"转换"动作定义得
  不够可执行，模型就会退回到它能确定不出错的操作——什么都不做。

## F-012 · Ground-truth 拒绝后的重试会换壳但不换病灶——同一失败指纹在同一工具的连续尝试中原样复现

- **日期**：2026-07-11 · **状态**：观察中
- **模型**：deepseek-v4-pro（BUILD，Mode 3）；Playwright 无头浏览器地面真值门（非 LLM，见 F-001）
- **声明**：Mode 3 工具被 Playwright 地面真值拒绝（"DOM did not react to input"）后，
  BUILD 收到的重试反馈只有症状级数据（fields_filled / text_changed / 节点数），不含
  "哪一行 JS 没有绑定事件监听器"或"处理函数没有写回 DOM"这类机制级信息。结果：同一
  工具连续 4 次重试，DOM 节点总数从 34 变为 52（说明代码结构确实被重写，不是简单
  复制），但核心病灶（`text_changed` 恒为 `False`）原样复现了 4 次。
- **证据**：质量账本 2026-07-11 同一工具连续 4 条记录：
  `fields_filled=4, text_changed=False, nodes 34->34`（×3）、
  `fields_filled=4, text_changed=False, nodes 52->52`（×1）。
- **应对**：Playwright 探测器需要从"前后节点数对比"升级为机制级诊断——例如检测目标
  input 元素是否绑定了 change/input 监听器，若绑定了，监听器函数体是否实际写入了
  目标节点的 textContent/innerHTML；重试反馈应把这条诊断原文喂给 BUILD，而不是复述
  "DOM 没反应"这一症状描述。
- **推论**：地面真值门解决了"要不要相信模型自称做到了"（F-001），但还没解决"模型
  如何修复它做错的具体机制"——症状级反馈只能触发换壳式重写，机制级反馈才能触发
  针对性修复。这是 F-001 之后地面真值系统需要补的下一层，样本量仅 1 个工具、4 次
  尝试，需要更多独立事件才能从"观察中"升级为"确认"。
## F-013 · 自我修改流水线的写入路径需要与外部编辑同等的语法防护，两者不会自动共享

- **日期**：2026-07-12（事故）/ 2026-07-17（修复）· **状态**：确认
- **模型**：deepseek-v4-pro（周进化文本生成，写入 `lili_engineering.py` /
  `lili_blindspot.py` / `lili_soul.py` 三个 carrier 文件）
- **声明**：项目已有 PostToolUse hook 在 Claude 编辑 `src/*.py` 后自动跑
  `ast.parse()` 校验语法（见 CLAUDE.md Phase 8），但周进化管道自己把模型生成的
  反思文本（工程教训示例、盲点分析）拼进同一批 `.py` 文件的三引号字符串字面量时，
  完全没有等价校验。2026-07-12 一次运行生成的工程教训示例里包含字面
  `"""..."""` docstring，提前闭合了外层三引号字符串，导致两个文件从那次提交起
  持续 `SyntaxError`，直到次日跑测试才被发现（`test_prompts.py` 的 import 链
  `lili_prompts` → `lili_blindspot` 报错）。防护存在于"Claude 改代码"这条路径，
  不存在于"Lili 自己改代码"这条路径——两条路径共享同一份被写入的文件，却不共享
  同一份防护，是因为防护被实现为外部工具（hook）而不是写入函数本身的前置条件。
- **证据**：CLAUDE.md Pitfalls Logged 对应条目；提交 `0955e35` 的
  `_sanitize_embedded()`（转义三引号与结尾反斜杠）与 `_guarded_write()`
  （写入前 `ast.parse()` + `exec()` + 必需符号名检查，任一失败则保留上周文件、
  拒绝写入）；提交信息记录"verified end-to-end with the exact 07-12 killer
  input"，即用引发事故的原始 payload 重放，确认新代码路径下文件仍能正确解析、
  导入、内容保留；新增 10 个单测覆盖净化器与 guarded-write 的拒绝/接受矩阵。
- **应对**：任何"系统改写自己代码"的写入函数，防护必须内建在写入函数本身
  （写入前双重校验：语法 + 语义/符号完整性），不能依赖外部编辑钩子——因为自我
  修改的调用路径天然不经过那个钩子。
- **推论**：F-007 指出"把 HTML/JS 塞进 Python 字符串是稳定的语法错误源"，这条
  发现是它的自指版本——当"塞进字符串"的内容来自系统自己的反思输出而不是外部
  代码时，同一类错误一样会发生，且更危险，因为没有人在旁边跑 hook。自治系统里
  "写入自己源码"的每一处，都应该被当成需要 ground truth 校验的边界，而不是
  信任内部调用。

## F-014 · 重试到成功为止的逻辑对"本质上不可能"的任务没有退出条件——会无限期消耗主循环

- **日期**：2026-07-14 ~ 07-16（事故）/ 07-16（修复）· **状态**：确认
- **模型**：与具体生成模型无关——deepseek-reasoner(R1)/deepseek-v4-pro 在 SPEC/BUILD
  阶段反复尝试同一 Issue，问题不在模型输出质量，在管道的终止条件设计
- **声明**：Issue 委托流程（见 CLAUDE.md Issue Commission Flow）原先的逻辑是
  "每天重试，直到出现 `lili-built` 标签"，对可行任务是合理的重试语义，但对
  **本质上超出单文件沙盒能力**的任务（Issue #5：整理手机截图，需要视觉 AI +
  OCR，而工具运行环境是无网络、无后端的单文件 HTML/JS）等价于死循环——因为
  `lili-built` 永远不会出现，管道会一直把每天的构建配额耗在同一个不可能任务上，
  而不会退回到自由选题。
- **证据**：质量账本"repeat-offender concepts"统计显示该截图整理概念失败 9 次
  （`lili_ledger_report.py` 输出）；日记技术备注确认 2026-07-14~16 三个连续
  rest day 均源于同一委托的重试；提交 `5d0091e` 标题明确点出根因
  "Issue #5 ... consumed 3 consecutive days (07-14~16) because commissions
  retry daily until lili-built, forever"。
- **应对**：新增 `lili-blocked` 标签与两级失败响应——第一次失败发诚实的进度
  说明评论，第二次失败发能力边界说明并打上 `lili-blocked`，之后
  `fetch_tool_requests` 跳过被拦标签的 Issue，管道当天回到自由选题；Issue 本身
  保持打开（暂停而非放弃，未来能力扩展后可重新评估），不自动关闭。
- **推论**：这与 F-006（单文件工具不能承诺外部世界）是同一类"部署约束必须写进
  硬闸"问题的另一面——F-006 管的是"模型设计阶段会不会许下做不到的承诺"，这条
  管的是"当承诺已经许下且不可能兑现时，管道要不要有止损机制"。自治系统里任何
  "重试直到成功"的循环都需要显式的失败计数上限和退出路径，否则任务是否可行
  这件事只能靠管道被目睹"卡住"来发现，而不是被设计出来预防。

---

## F-015 · 重试反馈的关键词路由存在自我遮蔽——更具体的诊断分支被更早、更宽泛的分支吃掉

- **日期**：2026-07-04（引入）~ 07-24（发现）· **状态**：确认，已修复
- **模型**：deepseek-v4-pro（生成）；qwen3.7-max（Critic，产出了触发遮蔽的措辞）
- **声明**：F-009 的"填充物"专项重试分支（07-04 加入）用关键词 `hallucinat/invented/
  padded` 匹配，但代码顺序上排在更早、更宽泛的"generic/static"分支**之后**。Critic
  给出的真实拒绝理由经常同时包含"generic"（如"a generic 'Conclusion' chapter"）和
  "hallucinat"，于是被前一个分支截获，从未走到为它专门写的、更具体的"删除任务"反馈。
  这个 bug 隐藏了 20 天，因为两个分支的反馈都"合理"，日志里看不出路由错了。
- **证据**：`tests/test_retry_routing.py` 新增的路由回归测试首次运行即失败
  （`test_padding_not_shadowed_by_identical`）；用真实账本核对，07-04/07-06/07-10/
  07-16 共 4 条应属 padding 类的失败记录，原因文本同时含"generic"和"hallucinat"，
  证实生产环境确实一直在走错分支。同一天（07-24）的浏览器验证连续 4 次拒绝同一
  "字距微调"工具、原因逐字重复，是这类"反馈不具体→模型无法收敛"问题的独立佐证。
- **应对**：把 padding 分支移到 generic/static 分支**之前**（更具体的诊断应优先于
  更宽泛的）；新增 `tests/test_retry_routing.py` 把整条 elif 链的匹配顺序做成
  独立、带真实历史用例的回归测试，而不是每次改动后手动跑一次性验证脚本。同时给
  浏览器地面真值探针加上控制台报错捕获（`console_errors=`），让"DOM 没反应"这类
  失败的重试反馈从"泛泛建议"变成"具体检查这几个绑定点"。
- **推论**：字符串关键词路由的分支顺序是隐性契约，且极易被后续新增分支破坏而不报错
  ——每次新增 elif 分支都必须验证它是否会被更早的分支吃掉，也可能反过来吃掉更晚的
  分支。这类系统的正确形态是显式优先级表 + 回归测试，而不是新增代码时凭直觉插入
  一个 elif。

## F-016 · 地面真值探针本身有验证盲区——只认文本输入，不认选择式交互

- **日期**：2026-07-24 · **状态**：确认
- **模型**：deepseek-v4-pro（生成，Design Alchemy 连续两天设计"从预设选项选一个语气/
  意图"的选择式工具）；探针本身是我们自己的 Playwright 脚本，非 LLM
- **声明**：`_browser_interactivity_check` 的填充逻辑只处理
  `textarea/input[type=text]/contenteditable`，从不处理 `<select>`、单选/复选框、
  或"选项卡片"类点选控件。当工具的真实交互模型是"点选一个选项"而非"打字"时，
  探针填不进任何输入（`fields_filled=0`），无论 JS 是否真的工作，`text_changed`
  永远是 False——这不是工具假，是**验证工具本身认不出这种真实的交互形态**。
  与此同时捕获到的控制台报错"Clipboard write permission denied"是无头浏览器
  沙盒本身对剪贴板 API 的限制，与代码正确性无关，若原样喂给模型当作"要修的
  bug"，会浪费一次重试在不可能修好的东西上。
- **证据**：2026-07-24 全天 5 次以上失败原因均为 `fields_filled=0`，且工具描述
  明确是"select an intent/tone from options"类选择式 UI；两条失败记录的
  `console_errors` 字段均为剪贴板权限错误，与"DOM 没反应"无因果关系。
- **应对**：探针新增选择式交互驱动——对 `<select>` 切换到非默认选项、勾选单选/
  复选框、点击 `[role=option]/.chip/.option/[data-value]` 等选项类元素，均触发
  `input`/`change` 事件后再判定 DOM 是否变化；同时新增
  `_is_environment_noise()` 白名单过滤已知的无头浏览器环境噪音（目前含
  clipboard），不再把环境限制误报成代码 bug。重试反馈加入"若交互模型是选择而非
  打字"的专项检查点。
- **推论**：给 AI 生成的产物设计"地面真值"验证器时，验证器自己的交互覆盖面
  必须至少覆盖被验证系统能设计出的所有交互形态——否则验证器的盲区会被系统
  当作"这类交互不存在"的假象，反而定向淘汰了某一类可能完全正确的设计。
  验证器本身也需要像被验证的代码一样接受"过度自信/覆盖不全"的审视。

## F-017 · 真正的可靠性分界线是"价值是否依赖算法深度"，不是输出格式

- **日期**：2026-07-25（基于 2026-06-27 ~ 07-24 共 311 次尝试）· **状态**：确认
- **模型**：deepseek-reasoner(R1) 写 SPEC、deepseek-v4-pro 写 BUILD、qwen3.7-max 评审
- **声明**：按输出格式统计 28 天全部尝试，通过率分布为——
  E 环境型 11%（1/9）、D 实时画布 8%（6/75）、A 文本分析 3%（6/184）、
  **B 多字段表单 0%（0/25）、F 生成器+内联编辑 0%（0/18）**。
  这推翻了 F-002 的"纯函数比 HTML 可靠"，并给出更本质的分界线：
  **工具的价值是否依赖对用户文本的深度分析**。
  - 依赖（评分、判断、提炼洞察）→ 约 3%，且通过者中一半带 Critic 标注的
    "没有真实算法深度"。原因是单文件、无模型、无语料的工具**真的做不了深度语言分析**。
  - 不依赖，价值在做工与在场感（可调的视觉、活的乐器、自己运行的器物）→ 8-11%，
    且不带瑕疵标注。因为这类工具的"深度"是交互与视觉的做工，模型确实能做。
- **证据**：`lili_ledger_report.py` 28 天聚合 + 按 format 字段分组统计（311 次尝试，
  13 次通过）；13 次通过中 7 次（54%）带 `[Shipped with minor flaw]`，其瑕疵原文包含
  "no real algorithmic depth"（3 次）、"does nothing the user couldn't do in 10 seconds"
  （2 次）、"a professional would be embarrassed"（1 次）——全部集中在分析型工具。
- **应对**：① B/F 两个零产出格式在 SPEC prompt 中禁用，并在 `validate_spec` 加确定性
  改写兜底（B→A、F→D），因为模型有无视格式指令的前科；② SPEC prompt 写入实测通过率
  与新的分界线，明确"若摩擦点能被一个做得好的器物解决，优先 D/E"；③ 该偏好与 RULE 16
  （物理世界情感基础）本就一致，现在有统计支持。
- **推论**：给自治生成系统做能力画像时，"输出格式"是表象维度，容易因早期小样本
  产生错误结论；真正需要测量的是**任务对模型薄弱能力（此处为深度语义理解）的依赖度**。
  同一个格式下，依赖度高的任务和低的任务表现可以差 3 倍。

## F-018 · 所有质量门都在检查"形式"，没有一道门检查"承诺"——工具能声称做 X 却完全不做 X 而全绿通过

- **日期**：2026-07-28（事故）~ 2026-08-04（修复）· **状态**：确认，已修复
- **模型**：deepseek-v4-pro（生成 SVG Path Purifier）；qwen3.7-max（Critic，判定"clean"未发现）
- **声明**：浏览器地面真值探针检查"DOM 动了没"，Critic 检查"看起来专业吗"，
  test_main.py 检查"输出非空吗"——**没有任何一道门检查"工具在 README/SPEC 里
  声称做的那件具体事，实际做没做到"**。"SVG Path Purifier"声称移除 `<g>` 标签上的
  `fill="none"` 等属性，README 写明、SPEC 写明，但 ElementTree 处理默认命名空间的
  经典坑（`root.iter('g')` 因为真实标签名是 `{ns}g` 而永远匹配不到任何元素）导致
  删除逻辑从上线第一天起就是空转，属性原封不动。Critic 给了 3.0 分"clean, well-
  structured"通过，因为它看的是代码结构和输出格式是否工整，不是"承诺的事有没有
  发生"。持续 7 天无人发现，直到人工用真实场景手动测试。
- **证据**：`02_Toolbox/Design Alchemy/2026-07-28_SVG_Path_Purifier` 修复前 commit；
  人工复现：对含 `<g fill="none">` 的测试 SVG 跑 `process()`，输出中 `fill="none"`
  原样保留，且额外产生 `ns0:` 命名空间前缀污染（同一个 bug 的另一症状）。
- **应对**：SPEC 新增强制字段 `MUST_NOT_CONTAIN` / `MUST_CONTAIN`——要求对
  `TEST_INPUT` 的实际输出做出至少一条可机械核验的具体承诺（不能两者都为空）；
  `validate_tool()` 真实执行 `process(TEST_INPUT)`，用字符串包含检查核对承诺是否
  兑现，失败则精确报告"承诺删除的 X 还在输出里"或"承诺产出的 Y 没有出现"，
  而不是泛泛的"质量不足"。用真实的 SVG Purifier bug 模式写了端到端回归测试
  （`tests/test_promise_check.py`），证明修复前的代码会被新机制拦下、修复后的
  代码会通过。
- **推论**：验证链上的每一道门都在回答"这段代码/输出看起来怎么样"，没有一道门
  在回答"它做到自己说要做的事了吗"——这是两个完全不同的问题，前者可以无限打磨
  规则也测不出后者。**承诺与实现的核验必须是机械的、针对具体声明的，不能指望
  质量评分或代码审美类的检查顺带发现它。**

## F-019 · Harness 两处结构性改造：执行反馈内循环 + 差分测试（待生产数据验证）

- **日期**：2026-08-04 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：deepseek-v4-pro（BUILD/自纠正）；两处改动均为 harness 侧机制，非模型能力发现本身
- **声明**：诊断此前的失败数据发现，311+ 次尝试里的失败集中在两类"人类看一眼就能
  发现"的问题：① 逻辑/语法/命名空间类 bug（F-018 SVG Purifier 是典型）——根因是
  BUILD **一次性盲写代码，写完前从未真正运行过**；② "输出不依赖输入"的泛化壳
  （占失败 32%，`fake-static`+`identical-output`+`generic-shallow` 三桶合计）——
  此前完全靠 Critic 的主观判断识别，没有机械证据。两个都不是"模型不够聪明"，
  是**harness 从未给模型机会看见自己的真实输出**。
- **应对**：
  ① **执行反馈内循环**（`self_correct_code`，`lili_pipeline.py`）：BUILD 写完代码后，
  在进入昂贵的 Critic/浏览器地面真值/质量评分之前，先本地做语法检查 + 真实执行
  `process(test_input)` + 核对 F-018 的承诺（MUST_CONTAIN/MUST_NOT_CONTAIN）。
  代码本来就没问题时零 API 调用（本地检查通过直接放行）；发现问题时把**真实观测到
  的输出/报错**（不是猜测）喂回去，最多 2 轮针对性修补，再进入完整验证链——完整
  验证链依然是最终把关者，这道内循环失败也不会让坏工具漏出去，只是让更多尝试
  "带着已经修好的代码"进入验证链。
  ② **差分测试**（`_differential_test`，`lili_validators.py`）：用两个刻意选择的、
  完全不相关领域的输入（科技裁员话题 vs 酸种面包话题）各跑一次 `process()`，
  和主测试输入的输出三方两两比对相似度（`difflib.SequenceMatcher`），全部
  >85% 相似则机械判定"没有真正处理输入"，给出量化证据（具体相似度百分比 +
  最长重复片段原文），而不是 Critic 的模糊印象。对执行失败的次要输入 fail-open
  跳过（不是所有工具都能吃任意英文段落，比如只接受合法 XML 的工具）。
- **证据**：`tests/test_self_correct.py`（7 个测试，覆盖零调用快速路径、语法/崩溃/
  承诺三类问题的检测与修补、二轮复检真的抓到"修了个寂寞"的补丁、最大轮次耗尽后
  优雅降级）、`tests/test_differential.py`（5 个测试，含用真实 fixture 跑通的
  端到端回归：模板壳工具被拒、真计算工具通过、次要输入崩溃时正确 fail-open）。
  95 个测试全绿，本地 3.13 与真实 3.11 环境双重验证。**尚未在生产流水线跑过一次**，
  本条目的"有没有用"结论要等明天起的真实运行数据。
- **推论（待验证）**：如果预测成立，逻辑类失败应该大幅减少（模型在提交前就看见并
  修好了），泛化类失败应该从"Critic 偶尔抓到"变成"每次都被量化证据抓到"。
  真正的风险是差分测试的两个固定对照样本可能对某些合法工具产生误杀（比如一个
  就该对大多数输入给出相似框架输出的工具），需要观察首周数据里有没有这类假阳性，
  必要时把阈值从 85% 调松或允许 SPEC 声明"本工具的合理相似度上限"。

## F-020 · 每周自进化的密封测试集 + 补丁式门槛（待生产数据验证）

- **日期**：2026-08-03 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：本条目是纯 harness 结构改造，不涉及具体模型能力发现；`gate_evolution_proposal`
  运行 `deepseek-v4-pro`/`qwen3.7-max` 生成的每周反思内容中新增的 JSON 提案，
  但门槛本身是纯 Python 确定性回测，零 API 调用
- **声明**：诊断发现每周自进化此前只输出**未经核验的散文规则**，直接写入下一周的
  BUILD prompt（`LILI_ENGINEERING_LESSONS` 等）——没有任何机制核对"这条规则说的
  和真实台账数据是否吻合"，也没有任何机制事后核对"这条规则生效后通过率有没有
  变化"。连续 4 周（2026-W28~W31）台账通过数 4→3→4→3，看不出自进化对通过率有
  可观测的正向影响，理由之一正是规则本身从未被核验过。参考 EverMind/Raven 的
  Evolver 模式（诊断失败轨迹 → 设计候选补丁 → 只有通过针对密封测试集的统计
  门槛才允许晋升），但 Raven 的补丁是确定性代码、可以真实重放测试；我们的自
  进化产出是喂给随机 LLM 调用的散文规则，无法用同样方式确定性回测。
- **应对**：把每周自进化的输出从"纯散文"拆成两条并行通道——散文继续保留（人味、
  可读性），另外新增一条**结构化、机器可核验的"旋钮"通道**（`---EVOLUTION_KNOBS---`
  标签，JSON 格式：`deprioritized_categories`/`banned_concepts`/`format_bias`）。
  新模块 `lili_evolution_gate.py` 对这条通道做**确定性回测**（零 API 调用，纯
  Python 统计）：
  - `deprioritized_categories`：只有当该分类的实测通过率确实低于全场中位数才
    接受，且设置安全上限（默认不超过全场 34%）防止自进化把自己逼到无分类可选
  - `banned_concepts`：只有当该概念确实是台账里的"惯犯"（3 次以上失败）才接受
  - `format_bias`：只有偏好方向与该格式实测通过率相对中位数的方向一致才接受
  - 任何一项回测不通过，单独丢弃并记录原因（不是全批推翻），保留其余站得住的
    提案——一个部分合理的提案不该因为一条不合理就被整体否决
  - 门槛还叠加了现有 108 个单元测试作为行为安全网（`run_test_suite()`）：测试套件
    不是绿的，本周所有旋钮提案一律拒绝，不管回测结果多好看
  - 通过门槛的旋钮写入 `lili_evolution_knobs.json`（纯数据文件，不是可执行 Python，
    风险类别天然低于 F-013 那类"散文写进代码载体文件"的问题），由日常 SCOUT/SPEC
    prompt（`lili_prompts.py`）读取并叠加到已有的 `banned_cats`/格式路由提示上——
    不是替代原有 2 天/3 次失败的启发式，是在其基础上再叠加一层有数据支撑的信号
- **证据**：`tests/test_evolution_gate.py`（13 个测试，覆盖回测通过/拒绝/安全上限/
  数据量不足/JSON 持久化/损坏文件容错/测试套件红灯时门槛整体拒绝的完整矩阵）。
  108 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。**尚未跑过一次真实的每周
  自进化**——下一次自然触发是 2026-08-09（周日），这之前 F-019 的执行反馈内循环
  + 差分测试有约 6 天干净的独立观察窗口，两者的生产效果不会互相混淆归因。
- **推论（待验证）**：如果设计成立，应该能观察到"提案被拒绝"本身成为一种有信息量
  的信号（说明 LLM 对本周数据的诊断有偏差），而不是每周无条件全盘采纳。真正的
  风险是回测阈值本身可能过严或过松——分类通过率的"中位数以下才算差"在分类数量
  很少时统计意义有限，需要在旋钮机制运行数周、分类样本量上来后重新校准安全上限
  与阈值。

## F-021 · 三处诊断出的结构性缺口：类别拦截无下限、自纠正内循环漏了 Mode 3、概念级黑名单太慢

- **日期**：2026-08-03 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构修复，不涉及具体模型能力发现
- **声明**：对 28 天真实台账（298 次尝试，通过 14 次）做失败桶分解后发现，
  `fake-static` + `browser-ground-truth` 两桶合计 140/298（47%），`no-algorithmic-depth` +
  `hallucination-padding` + `generic-shallow` + `identical-output` 合计 84/298（28%）——
  语法错误只有 4 次，说明"写出能跑的代码"早就不是瓶颈，真正的瓶颈是"交互是假的"和
  "选题本身做不到"。深挖这两桶，找到三个具体的结构性缺口：
  1. **类别拦截三层叠加无下限**：`recent_cats`（最近 2 天用过）/ `failed_cats`
     （3 天内失败 2+ 次）/ F-020 新增的 `evolved_cats`（每周门槛淘汰）各自独立判断
     "该不该禁用某类别"，取并集时互不知道对方存在。全场只有 4 个类别（Healing 还被
     规则单独压到 20% 以内），三层叠加完全可能把可选类别压缩到只剩一个。数据印证：
     Office Automation 同时是产量最大（166/298，56%）和通过率最差（3%）的类别——
     跟"被迫塞进去的题目"这个解释吻合，而不是"这个类别天生难做"。
  2. **自纠正内循环（F-019 harness plan A）结构性漏掉了占失败 47% 的那个桶**：
     `self_correct_code` 只会给 Mode 1/2 文本工具做"写完自己先看一眼"（本地跑一遍
     `process()` 核对文本），Mode 3 交互工具完全没有这个待遇——真浏览器点击检测
     （`_browser_interactivity_check`）只存在于后面昂贵的 `validate_tool()` 链里，
     模型永远看不到"你点的按钮没反应"这个真实反馈来自己改，只能在提交之后被动挨拒。
  3. **概念级黑名单反应太慢**：`repeat-offender` 列表里排第一的"手机截图整理"失败了
     9 次——这正是当初被 commission 断路器（F-014）判定"结构性不可能"（需要视觉 AI +
     OCR，单文件沙盒工具做不到）、标了 `lili-blocked` 的同一个概念。断路器只管 Issue
     委托来的任务，不管 SCOUT 每天自由选题；F-020 的 `banned_concepts` 门槛又要等周日
     才生效——两条路径都管不到"SCOUT 每天自己重新提出同一个已判死刑的概念"这件事。
- **应对**：
  1. `lili_prompts.py` 新增 `_apply_category_floor()`：合并三层来源时保证至少留 2 个
     类别可选，优先保留被多个来源同时标记的类别（证据更强），砍掉单一来源标记的。
  2. `self_correct_code`（`lili_pipeline.py`）新增分支：当 `spec["mode"]` 以 "3" 开头，
     在现有的语法/执行检查之后额外跑一次 `_browser_interactivity_check`，把"DOM 没反应"
     也当作真实观测反馈喂给模型做针对性补丁——复用已有函数，不是新写检测逻辑。
     Fail-open 设计原样保留：Playwright 不可用/崩溃时不算缺陷，跟 `validate_tool()`
     里那道检查完全一致的容错策略。
  3. `lili_prompts.py` 新增每日概念级检查：直接复用 F-020 里已经写好的
     `_repeat_offender_concepts()`（同样的"3+ 次失败"判定，同样匹配工具描述前 60
     字符），不经过每周门槛的 LLM 提案+回测流程，每天在 SCOUT 选题前跑一次——因为
     这个检查本身是纯机械的，不需要等模型提案。
- **证据**：`tests/test_category_floor.py`（5 个测试，覆盖无拦截/低于下限直通/超出
  下限裁剪/多来源证据优先保留/绝对不超下限）；`tests/test_self_correct.py` 新增 3 个
  测试（Mode 3 静态 HTML 触发补丁、Playwright 不可用时 fail-open、Mode 1 工具完全不
  触发浏览器检查）；`tests/test_prompts.py` 新增 2 个测试（台账有惯犯概念时注入提示、
  台账干净时不注入）。118 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证——3.11 venv
  里 Playwright 包已安装但浏览器二进制未下载，暴露了一处真实的测试脚手架 bug（跳过
  条件只匹配了 "playwright unavailable" 这一种报错文案，没覆盖 "browser run failed"
  这种），修正后两个环境都稳定通过。
- **推论（待验证）**：三个修复分别针对三个不同的机制层级（选题约束 / 执行反馈 / 黑名单
  时效），理论上应该分别改善类别分布均衡度、Mode 3 通过率、概念级惨案复发次数。真正
  的验证要等生产数据——本条目跟 F-019/F-020 一样，是"机制已验证但效果未验证"的状态，
  需要观察接下来 1-2 周的台账数据里这三个信号是否真的移动。

## F-022 · "永不重复话题"+运行 190 多天，把选题结构性推向小众长尾

- **日期**：2026-08-03 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness/prompt 结构修复，不涉及具体模型能力发现
- **声明**：项目owner 直接指出最近很多选题"没有明确意义、难以标准化、不是大部分人的需求"，
  以手动触发跑出的真实案例为证——SPEC 选了"给发票上含糊的日期打标签，区分权责发生制/
  收付实现制报税用途"这样一个交互向导。诊断发现根因是两条规则叠加的结构性必然：
  ① FRICTION LOCK 要求"精确到具体的人和瞬间"；② 内存机制要求"绝不能重复过去做过的话题"。
  运行 190+ 天后，大部分人会遇到的通用摩擦点（会议记录整理、文字稿清理、批量改名）早就
  写过一遍，为了同时满足"新鲜"和"精确"，SCOUT 被迫的路径只剩往长尾钻——越挖越偏，因为
  中心地带已经写完了。"精确"被悄悄等同于了"小众"，而从来没有一道检验在管这两者的区别。
- **应对**：
  1. SPEC 新增强制字段 `COMMON_ROLES`（Rule 20）：要求列出 **至少 2 个明显不同的职业角色**
     （不是同一人设的变体，比如"自由摄影师"和"自由插画师"算同一人设）都会撞上这个精确的
     摩擦点。`validate_spec` 机械核验：少于 2 个角色、或角色本质重复（大小写/去空格后相同），
     直接打回，不进 BUILD——不依赖模型自己判断"这个够不够通用"（它显然判断不准，不然也
     不会选发票日期这种），而是逼它具体列出角色，列不出来就是硬证据。
  2. 把 SCOUT 提示词里"绝对不能碰过去做过的任何话题"的措辞从"话题级"松绑为"具体工具级"：
     只禁止字面重建同一个工具（同样的输入/输出/算法），明确告诉模型"重新用不同算法角度
     深挖一个通用类别（会议记录、文字稿清理、简报撰写）"是可以的，好过为了避免重复硬找一个
     没人做过但小众的新名词。
- **证据**：`tests/test_universality_gate.py`（8 个测试，覆盖 2 个/3 个不同角色通过、0 个/
  1 个角色拒绝、精确重复角色拒绝、字段解析的逗号切分/缺失默认空/"none"归零）。126 个测试
  全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果设计成立，接下来几周的选题应该能看到"围绕通用类别深挖"的比例
  上升，"手机截图整理""发票日期标签"这类窄众概念的出现频率下降。真正的验证要等生产数据——
  跟 F-019/F-020/F-021 一样是"机制已验证但效果未验证"的状态。一个需要留意的风险：
  `COMMON_ROLES` 本身是模型自己写的，如果模型学会"编两个听起来不同、其实还是同一类人"的
  角色来蒙混过关（比如"独立记者"和"自由撰稿人"），这道检验就会变成走过场——这是纯文本
  匹配无法完全防住的，需要观察后续台账里 COMMON_ROLES 的实际内容是不是在应付了事。

## F-023 · 类别失衡的真根因不是拦截逻辑，是 Office Automation 自己的描述文本写成了无边界兜底类

- **日期**：2026-08-05 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 prompt 文本修复，不涉及具体模型能力发现
- **声明**：owner 怀疑"最近几天连续休息是不是新加的机制太严"，诊断台账后发现拒绝原因
  全部是 Critic 判断的老失败类型（幻觉编造、缺少必填字段、原样照抄输入），跟 F-019~F-022
  新加的门槛无关。但诊断过程中发现一个真实、独立的问题：28 天台账里 Office
  Automation 占了 175/304 次尝试（58%），且**这三天（08-03~08-05）SCOUT 在完全自由选择的
  情况下，连续三天都选了 Office Automation**——F-021 加的类别拦截下限（`_apply_category_
  floor`）对此完全没有效果，因为它只能防止"类别被过度禁用导致被迫选剩下一个"，而这次根本
  没有任何拦截在起作用，是 SCOUT 主动、自愿选的。查了 SCOUT 提示词里 Office Automation 的
  类别描述文本，开头就是"ANY repetitive professional production task"——用"ANY"这个词
  把自己写成了唯一一个无边界的兜底类，另外三个类别的描述都是限定在具体生活领域（学习/
  创作/疗愈）。而且描述里的"transcript summarisers"跟 Education Evolution 重复，
  "research-to-outline / brief extractors"跟 Design Alchemy 的"brief writing / spec-
  handoff automation"重复——任何模糊的摩擦点，语义上都最容易落进这个描述最松、覆盖面
  最广的类别，跟拦截逻辑无关，是类别定义本身在结构性地吸走流量。
- **应对**：把 Office Automation 的描述从"ANY ... task"改写成跟另外三个一样具体、有边界
  的定义——收窄到"行政/后勤类事务性工作"（发票合同文书、日程协调、费用记账、文件命名整理、
  批量重命名、往来邮件模板），并显式排除跟其他类别重叠的项（"NOT transcripts... that's
  Education Evolution"、"NOT briefs... that's Design Alchemy"）。
- **证据**：`tests/test_prompts.py` 新增 `MissionAreaScopeTests`（3 个测试：确认不再包含
  "ANY repetitive"这种无边界措辞、确认显式排除了 transcript/podcast、确认显式排除了
  brief/handoff）。129 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果诊断成立，接下来几天 SCOUT 的类别分布应该会更均衡，Office
  Automation 的占比应该从 58% 明显回落。这跟 F-021 的类别下限修复是互补而非替代关系——
  F-021 防止"被迫选剩一个"，F-023 防止"自愿选最松的那个"，两种机制解决的是不同的失衡
  来源，此前只做了前者，后者才是这三天数据里真正在起作用的那个。

## F-024 · 账单故障和创造性 rest day 长得一模一样——诊断信号被自己的诗意文案盖住了

- **日期**：2026-08-07 · **状态**：确认（真实事故 + 机械可复现的分类逻辑，非单纯推测）
- **模型**：纯 harness 结构改动，不涉及具体模型能力发现——事故本身是 Qwen（DashScope）
  账户欠费、DeepSeek 余额同时耗尽，跟任一模型的推理能力无关
- **声明**：2026-08-06 和 2026-08-07 两天，Qwen 因欠费、DeepSeek 因余额不足同时不可用，
  SCOUT 阶段的第一次调用就失败，F-019~F-023 加的所有下游机制（类别下限、自纠正、
  ground-truth 探针……）根本没有机会运行。但当天的日记只写了"Phase 1 failed - Qwen
  search and DeepSeek fallback both failed"，跟一次普通的"今天没有力气"式创造性 rest day
  用的是同一套诗意文案模板，肉眼完全无法区分。事故直到有人手动翻 GitHub Actions 原始日志
  才被发现——**诊断信息在系统里，但被展示层的一致性抹平了**。
- **应对**：`is_billing_error()`（`lili_llm.py`）识别已知的账单/欠费错误签名（Arrearage、
  Insufficient Balance、quota exceeded 等），`_call_qwen_search` 保存最近一次原始错误
  供调用方检查；`classify_scout_failure()`（`lili_pipeline.py`）区分"基础设施账单故障"
  和普通瞬时失败，`save_rest_day()` 对账单故障渲染明显不同的横幅（"基础设施故障 - 今天
  没有真正尝试"），日记文件和 README 精选条目都同步区分，不再跟真正的创造性/质量类
  rest day 共用同一套文案。
- **证据**：`tests/test_billing_outage.py` 新增 12 个测试（141 个测试全绿，本地 3.13 与
  真实 3.11 venv 双重验证）——`IsBillingErrorTests`（6 个：Qwen 欠费、DeepSeek 余额不足、
  OpenAI 风格 quota exceeded 均识别为账单错误，普通网络错误/普通 5xx/空字符串均不误判）；
  `ClassifyScoutFailureTests`（4 个：Qwen 单独账单错误触发分类、DeepSeek 单独账单错误
  触发分类、双方账单错误都写进原因说明、普通失败不触发账单分类）；`SaveRestDayVisibilityTests`
  （2 个：基础设施故障拿到不同横幅、普通 rest day 保留原有文案）。
- **推论**：这条修的是"事故发生后怎么被看见"，没有解决"事故发生前要先烧完一整轮
  SCOUT→SPEC→BUILD→Critic 才被发现"的问题——后者由两天后的 F-025 补上（pre-flight
  健康探测，在花任何真实 token 之前先用近乎零成本的探针确认 provider 可用）。两条
  是同一次账单事故催生的连续两步修复，分别对应"可见性"和"提前拦截"两个不同问题。

## F-025 · 低成本 pre-flight 探测 + 自纠正轮次上限提高（harness 架构讨论落地的第一步）

- **日期**：2026-08-07 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构改动，不涉及具体模型能力发现
- **声明**：F-024 修的是"账单故障发生后怎么可见"，没有解决"账单故障发生前要先烧完一整轮
  SCOUT→SPEC→BUILD→Critic→浏览器检测才发现"的问题——2026-08-06/07 那两天，每次运行都是
  完整跑了一遍全部阶段、烧了真实 token，最后才在 Phase 1 失败。同一次对话里也讨论了更大的
  架构问题：为什么模型能力持续提升，莉莉的产出质量却没有跟着涨——诊断是 BUILD 阶段本质上
  还是"一次性盲写 + 最多 2 轮文字补丁"的旧范式，没有让模型使用真正的"写代码→执行→看真实
  结果→自己判断下一步"的智能体循环，而这恰恰是前沿模型能力提升最集中体现的地方。
- **应对**（本次只落地了讨论中成本可控、当天可以验证的部分，完整的工具调用式 BUILD 重构
  留作后续独立工作）：
  1. `check_provider_health()`（`lili_llm.py`）：花几乎零 token（`max_tokens=1`）分别探测
     Qwen/DeepSeek 是否可用，在 `evolve()` 最开头、花任何真实 token 之前跑。
  2. `check_billing_outage_preflight()`（`lili_pipeline.py`）：只有**两个 provider 都因
     账单类原因不可用**才跳过整轮消耗——单个 provider 健康就放行，因为已有真实证据
     （2026-08-07 Qwen 欠费当天，DeepSeek 独自撑起 SCOUT/SPEC/BUILD/Critic fallback 并
     成功发布了一个真实工具 "Flyer Mosaic Finder"）证明单 provider 存活时不该提前拦截。
  3. `self_correct_code` 的默认轮次上限从 2 提到 4——不是无上限的智能体循环（那需要真正的
     工具调用式重构，成本讨论后判断风险/收益比现在不适合仓促做），只是在讨论中明确的
     "硬上限、可控成本"前提下，给模型多一点基于真实执行反馈收敛的机会。
- **证据**：`tests/test_billing_outage.py` 新增 `PreflightHealthCheckTests`（4 个测试：
  双 provider 账单故障时跳过、单 provider 健康时不跳过、双健康不跳过、双故障但非账单原因
  不跳过——最后一条防止把普通网络抖动误判成账单故障提前放弃一整天）；`test_self_correct.py`
  新增 `SelfCorrectDefaultRoundsTests`（2 个测试：默认值确实是 4、耗尽 4 轮的调用计数
  正确）。147 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果设计成立，未来再发生账单类故障时，浪费的 token 应该从"一整轮
  SCOUT→SPEC→BUILD→Critic"降到"两次几乎零 token 的探测调用"；轮次上限提高后，logic 类
  失败（F-018/F-019 覆盖的那类）里"2 轮没修完、其实第 3-4 轮能修完"的比例应该有所下降。
  完整的工具调用式 BUILD 重构（让模型自己决定何时停，而不是引擎硬编码固定轮次）仍是本条目
  未完成的部分，是下一步更大的架构工作，需要独立的设计和验证窗口。

## F-026 · 边界输入检查 + 按需检索历史成功案例（harness 架构讨论落地的第二步）

- **日期**：2026-08-09 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构改动，不涉及具体模型能力发现
- **声明**：接续 F-025 的架构讨论，落地当时排定的 #2、#3 两项（#1 完整的工具调用式 BUILD
  重构仍未做，属于独立的更大工作）：
  1. SPEC 阶段现在只用一条 `TEST_INPUT`（干净、理想化的示例）核验承诺，从来没检查过
     "如果输入更乱一点会不会崩"——一个只在干净示例上成立的工具是一种"happy-path 幻觉"。
  2. BUILD 重试时只喂散文规则和当前这次的报错文字，从来不给模型看一个真实成立的例子——
     散文规则是"应该怎么做"的抽象描述，具体案例是"这样做过、真的成立"的证据，但塞进每次
     调用都会增加 token 成本（上次成本讨论时已经明确要"按需触发，不要默认开启"）。
- **应对**：
  1. SPEC 新增可选字段 `EDGE_CASE_INPUT`——比 `TEST_INPUT` 更乱、更接近真实用户会输入的
     内容（缺字段、格式不规整、边界值）。`self_correct_code` 在主输入的 promise check 通过
     之后，**额外**对这个边界输入跑一次同样的 `MUST_CONTAIN`/`MUST_NOT_CONTAIN` 核验——
     复用已有的检查逻辑（提取成 `_check_promise()` 辅助函数），不是新发明一套断言体系。
     写 "none" 表示 TEST_INPUT 已经覆盖了这个工具的真实输入范围，不强制。
  2. `get_reference_tool_snippet()`（`lili_prompts.py`）：从 `lili_memory.json` 里挑一个
     真实发布成功的工具（优先同类别、否则取最近一个），截取一段 `main.py` 作为参照。
     **只从自纠正内循环的第 2 轮起才调用**——第 1 轮已经有 SPEC 可以参照，这是留给"一轮
     真实反馈后还是没修好"的情况，跟 #2 一样遵守"按需触发、不默认开启"的成本约束。
- **证据**：`tests/test_self_correct.py` 新增 `SelfCorrectEdgeCaseInputTests`（3 个测试：
  边界输入失败触发补丁、无边界输入时跳过额外检查零调用、"none" 视为未设置）和
  `SelfCorrectReferenceRetrievalTests`（2 个测试：第 1 轮不检索、第 2 轮检索且真的出现在
  反馈里）；`tests/test_prompts.py` 新增 `ReferenceToolSnippetTests`（5 个测试：空记忆返回
  空字符串、记忆读取异常不崩溃只返回空、优先同类别、无同类别时退化到最近一个、
  `max_chars` 截断生效）。157 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果设计成立，"只在干净示例上成立、真实用户输入稍微乱一点就崩"的
  这类 bug 应该在 self_correct_code 阶段被拦下更多，不用等到人工发现或 Critic 事后判断；
  反复失败 2 轮以上的尝试，第 3-4 轮的成功率应该比没有参照时更高（有真实案例可以对照结构，
  不是凭空再猜一次）。这两项都是"机制已验证但效果未验证"的状态，跟 F-019/F-020/F-021/
  F-025 一样需要生产数据观察。完整的工具调用式 BUILD 重构（#1 剩下的部分）仍待后续独立
  工作。

## F-027 · BUILD 从"一次性盲写+固定补丁轮次"改成真正的工具调用式调试循环

- **日期**：2026-08-09 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：`deepseek-v4-pro`（agentic 循环的执行模型；工具调用 API 是否被该模型稳定支持
  尚未经过真实生产流量验证，机制本身已用 mock 测试验证正确性）
- **声明**：这是 F-025/F-026 那次架构讨论里排定的 #1，此前两次都因为"完整重构风险大、
  应该先做成本可控的部分"而暂缓。owner 明确要求"把 1 做了"后完整落地。核心诊断没变：
  BUILD 阶段本质上还是"一次性写 200-300 行代码（看不到任何真实反馈）+ 最多 4 轮固定的
  文字补丁"，模型从来没有被授权在写代码的过程中自己选择"我要先跑一下看看""这个假设对不
  对我先验证一下"——而这恰恰是前沿模型能力提升最集中体现的地方（agentic 工具循环，不是
  单次生成质量）。
- **应对**：新增 `agentic_self_correct_code()`（`lili_pipeline.py`），用 OpenAI 兼容的
  原生工具调用协议（`tools=[...]`，DeepSeek/Qwen 客户端都是标准 `openai.OpenAI` 实例）
  替代硬编码的固定轮次循环：
  - 两个工具：`run_and_check(code, input_text)`——模型可以反复调用来验证一个假设，不限
    次数（受总轮次上限约束）；`submit_final_code(code)`——模型认为改好了就调用。
  - **模型的"提交"从不被直接采信**——每次 `submit_final_code` 都会用 `_execute_candidate()`
    （复用 `run_tool_output`/promise check/浏览器交互探针，跟 `validate_tool()` 同一套
    地面真值检查）机械复核一遍，通过才真正接受、结束循环；没通过就把失败原因喂回去，
    循环继续——这跟本 session 从 F-018 开始贯穿始终的"承诺不能靠模型自己说了算，必须
    机械核验"是同一个原则，只是这次用在了循环终止条件上。
  - **零 LLM 调用快速路径保留**：进入循环前先本地跑一次检查（含 EDGE_CASE_INPUT），已经
    干净直接返回，不浪费一次工具调用。
  - **硬性兜底**：整个函数包在 try/except 里，工具调用 API 本身报错（provider 不支持
    `tools` 参数、网络问题等）会**完整降级到已经跑了几周、验证充分的固定轮次版本**
    `self_correct_code()`——这条新路径只可能让结果变好，不会比现在更差。
  - 轮次上限默认 6（仍是硬上限，延续成本讨论的约束）。
- **证据**：`tests/test_agentic_self_correct.py`（7 个测试：零调用快速路径、
  run_and_check→submit_final_code 正常接受、模型谎报"修好了"但机械复核失败后循环继续、
  模型不调用任何工具直接放弃时优雅降级、EDGE_CASE_INPUT 在提交时也被复核、轮次耗尽不崩溃、
  工具调用报错时完整降级到旧版本且旧版本本身跑通）。164 个测试全绿，本地 3.13 与真实 3.11
  venv 双重验证——**未做真实 API 调用测试**，DeepSeek 的工具调用协议兼容性只在 mock 层面
  验证过结构正确，真实行为要等生产数据。
- **推论（待验证）**：如果设计成立，应该能看到部分尝试里模型主动调用 `run_and_check`
  验证假设而不是直接提交，且这类尝试的最终通过率应该高于纯文字补丁版本；如果 DeepSeek 的
  工具调用支持不稳定，兜底机制应该让整体表现不劣于 F-026 之前的基线——这条兜底路径本身
  也是本条目待验证的一部分（目前只在 mock 层面证明了"报错时会降级"，没有证明"真实 API
  报错的样子和 mock 的一样，能被同样的 except 捕获"）。

## F-028 · 验证通过 ≠ 值得使用——新增"可带走成品"门槛，主动牺牲部分通过率换真实价值

- **日期**：2026-08-09 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness/prompt 结构改动，不涉及具体模型能力发现
- **声明**：owner 直接指出根本问题——"现在工具质量不高，我自己也不大想试用"。这是本 session
  第一次有人指出一个此前从未被检验过的缺口：F-018 到 F-027 这一整条机制链（承诺核验、
  差分测试、浏览器地面真值、自纠正循环……）全部在回答"这个工具是不是坏的"，没有一个机制
  在回答"这个工具做出来之后值不值得打开第二次"——**验证通过和真实有用是两个正交的维度，
  这条链路一直在优化前者**。深挖后发现现有 SPEC 字段 `Q3_PASS`（"用户拿到输出 5 分钟内
  做什么"）本来就是在问这个问题，但 `validate_spec` 只检查它长度够不够（≥10 字符），从不
  检查内容是不是真的在描述一个可带走的东西——"获得洞察"这种空话能轻松通过。
- **应对**：不是发明新机制，是把 `Q3_PASS` 从"够长就行"升级成内容核验（Rule 21）：
  - 要求命中至少一个"可带走成品"关键词（复制/下载/粘贴/导出/文件/脚本/文本段落/表格……）
  - 命中"氛围/洞察/感受"类词而没有具体成品词，专门给出"这读起来像体验/感受，不是随用户
    带走的东西"的针对性提示（跟 `ALGORITHMIC_DEPTH` 拒绝"理解""洞察"的既有模式一致）
  - **明确承认这会跟现有可靠性策略冲突**：F-017 数据显示 Format E（氛围/环境类）通过率
    11%，是历史最高，FORMAT ROUTING 一直建议优先选它换更好的技术胜率——但 E 的定义就是
    "不需要输入"，天然不产出可带走成品。这道新门槛对 E 类工具会明显更难满足，短期内
    总体通过率大概率下降，这是**有意的取舍，不是回归**：用一部分通过率换真实价值，
    FORMAT ROUTING 章节里加了明确说明，不让模型误以为"通过率数据"和"这次选 E 值不值"
    是一回事。
- **证据**：`tests/test_validators.py` 新增 `PortableTakeawayGateTests`（4 个测试：多种
  具体成品表述全部通过、纯"氛围/感受"表述被拒、拒绝理由里含"体验/感受"针对性提示、
  没有成品词也没有可识别的氛围词时仍然被拒——防止关键词表以外的空话蒙混过关）。168 个
  测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果设计成立，短期通过率应该会因为 Format E/部分 Format C 更难满足
  而下降，这是预期内、健康的信号，不该被误判为系统变差；同时应该能观察到通过验证的工具
  普遍带有一个明确的可复制/下载/粘贴产出。真正的验证不是台账数字，是 owner 自己会不会
  开始愿意试用新发出来的工具——这条发现的最终判据本质上是主观的，跟本文件其他条目依赖
  机械证据的性质不同，需要特别说明。

## F-029 · 自进化闭环补上"事后复查"——不只验证提案，还要验证提案生效后有没有用

- **日期**：2026-08-09 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构改动，不涉及具体模型能力发现
- **声明**：owner 直接问"怎么让超级莉莉实现真正的自动化循环自进化"，诊断发现 F-020 的
  门槛只是个**单向阀门**，不是闭环——"提案 → 用历史数据回测 → 通过就应用"这条链路只验证
  了提案本身合不合理，从来没有验证过**应用之后，下一周的真实数据是不是真的变好了**。
  旋钮一旦通过门槛就会一直生效，没有任何机制回头检查"这个旋钮到底有没有用"，更不会
  自动撤销没用的旋钮——改动只会累积，不会因为没效果而收回。
- **应对**：`retrospective_check_knobs()`（`lili_evolution_gate.py`）在每周新提案覆盖
  旧旋钮**之前**，先拿当前生效的旋钮跟真实台账数据核对：
  - `deprioritized_categories`：核心逻辑是这个类别应用后**在总尝试量里的占比该降**——
    对比应用前后各 7 天窗口的占比，占比没降反而持平或更高，说明这条降权根本没被落实
    或者没起作用，直接从存活列表里剔除。
  - `banned_concepts`：应用后如果这个概念还在失败记录里复发 2 次以上，说明封禁没生效，
    剔除。
  - `format_bias`：偏好方向应该带动通过率往对应方向动——正向偏好通过率反而跌了，说明
    方向判断错了，剔除。
  - 应用不到 3 天的旋钮先跳过（"太早判断"），数据不足时保持原样不误杀，都写进日志。
  新增 `load_entries_range()`（`lili_ledger_report.py`）支持任意历史区间查询，是这次复查
  能对比"应用前 vs 应用后"两个窗口的基础，`load_entries()` 原来只支持"最近 N 天"。
  `run_retrospective_review()`（`super_lili_weekly_evolution.py`）接入每周流程，在
  `save_evolution_knobs_gated` 覆盖旧旋钮之前先跑复查、把结果写进日志，保持跟
  `backtest_knobs` 拒绝理由一样的透明度原则。
- **证据**：`tests/test_evolution_retrospective.py`（9 个测试，覆盖三类旋钮的"保留/剔除"
  判定、无 `week_start`/应用太短/数据不足三种边界情况）；`tests/test_retrospective_review_wiring.py`
  （2 个测试，确认接线正确）。179 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：这只是把"单向阀门"变成"循环"的第一步——目前复查结果只写进日志做
  透明度记录，还没有做到"自动把被剔除的旋钮从下周提案候选里硬性排除"（理论上下周
  `gate_evolution_proposal` 会用新数据重新回测，如果旋钮真的没用，新数据本身应该会让它
  过不了门槛，但这是隐式的，没有显式阻断"重新提同一个没用的旋钮"）。真正的对照实验
  （每天分流一部分尝试试用新旋钮 vs 不用，而不是全量应用后看趋势）仍是更大的下一步，
  这次没做。代码级自我修改（莉莉自己改 harness 源码）依然刻意不做——F-013 的教训还在，
  旋钮层面的"提案-验证-应用-复查"纪律要先经过几周真实周期验证，才该考虑更大胆的自主权。

## F-030 · SCOUT 阶段加"机械适配度"筛选——中等深度打法只对"可测量"的摩擦点管用

- **日期**：2026-08-09 · **状态**：观察中（纯 prompt 引导，无法机械核验，效果判断本身
  就依赖主观印象，比本文件其他条目更难验证）
- **模型**：纯 prompt 结构改动，不涉及具体模型能力发现
- **声明**：owner 追问"中等深度打法清单是不是做不出好工具"。诊断先给出反例（Hemingway
  App 就是纯"中等深度"——句长统计、被动语态规则检测，没有真正的语言理解，但确实是被
  广泛使用的好工具），推翻"中等深度=平庸"这个前提；但同时发现我最初的建议框架本身有问题：
  把"中等深度算法清单"当成随便挑一个往任何摩擦点上套的安全区，这才是真正的风险——同样的
  统计算法套在"这段话的品牌调性对不对"这种需要真实判断力的摩擦点上，做出来的必然是
  "技术上没错但没人想用的计算器"。真问题不是"清单里有什么算法"，是**这个具体摩擦点本身
  是不是客观可测量、不需要理解语义就能诊断的那类问题**。
- **应对**：在 SCOUT 阶段的选题环节新增"MECHANICAL FIT CHECK"（Rule 22）——在最终确定
  DESCRIPTION/SOLUTION 之前，先问"这是那种细心的人靠数一数、量一量材料本身就能诊断的问题
  （句子超过 40 词有几句、三个文件改名间隔 20 分钟），还是需要真正阅读和判断的问题（这段
  文字像不像我们的品牌调性）？"——前者才该往下做，后者应该换一个摩擦点，而不是硬凑一个
  统计算法上去回避判断力缺口。这是一道**选题筛选**，不是"把摩擦点硬塞进机械方案"的设计
  约束——放在 SCOUT 阶段（选题时）而不是 SPEC 阶段（已经选完题、算法接不上才发现），
  减少下游浪费的重试轮次，跟之前那些"事后拒绝"类门槛不同类。
- **证据**：`tests/test_prompts.py` 新增 `test_mechanical_fit_check_present`（确认 SCOUT
  提示词包含这道筛选和 Hemingway App 案例）。180 个测试全绿，本地 3.13 与真实 3.11 venv
  双重验证——**这条本质上无法用机械测试验证效果**，测试只能确认提示词文本存在，不能确认
  模型真的会照做，这点跟本文件其他基于台账数据回测的条目不一样，需要明确标注。
- **推论（待验证）**：如果设计成立，SPEC 阶段"算法深度不够/需要外部语料"类拒绝应该减少
  （摩擦点从选题起就避开了这个陷阱），选题整体应该更集中在结构性可测量的问题上。这是
  本次会话（2026-08-03 至 2026-08-09）新增的第 12 项机制（F-019 ~ F-030），全部尚未见过
  真实生产数据——owner 和我都同意接下来应该停下新增，先观察至少一周的真实台账，再决定
  哪些机制真的在起作用、哪些只是自我感觉良好的工程堆叠。

## F-031 · SCOUT 阶段"非空但不可解析"响应没有 F-020 已经给 SPEC 阶段配的兜底解析器，且不会重试

- **日期**：2026-08-13、2026-08-15，**2026-08-21 追加第三次** · **状态**：**确认**（结构性
  缺口本身已通过读代码确认；本条目原本的推论是"如果这是真实故障模式，未来几周应该继续
  以相近频率复现"——`01_Work_Log/2026-08-21-Diary.md` 技术备注逐字重复"Phase 1 (Scout)
  returned incomplete response."，是本模式第三次出现、跨两个自然周，符合"继续复现"而非
  "本周巧合"的判据，故升级为确认。**模型为什么没按格式回复——截断还是格式漂移——依然
  没有原始响应留存可查，根因本身仍未判定，升级的只是"这是真实存在的复现性故障模式"这一层**）
- **模型**：SCOUT 阶段模型为 `qwen-plus`（`_call_qwen_search`，DashScope web search，
  `max_tokens=4096`）；两次事件均未触发 DeepSeek fallback（该 fallback 只在
  `scout_content` 为空时触发，见 `lili_pipeline.py:870`，这两次 Qwen 都返回了非空内容）
- **声明**：本周（2026-08-10~08-16）6 个台账尝试之外还有 2 个 rest day（08-13、08-15），
  技术备注完全相同：`"Phase 1 (Scout) returned incomplete response."`——占本周日历天的
  2/7、占非进化日的 2/6。追到代码：`lili_pipeline.py:899-903` 在 `scout_content` 非空
  时调用 `parse_scout_response()`（`lili_validators.py:451-484`），该函数用精确的
  `---TITLE---`...`---CATEGORY---` 标签逐段切分，**没有任何兜底**——只要模型响应缺失或
  错位一个标签，对应字段及其后所有字段全部静默变成空字符串，`title`/`diary`/`solution`
  三者但凡有一个为空就直接判定"不完整"、写 rest day、当天结束，不重试、不用残余内容
  抢救。这与同一文件里 `parse_spec_response()`（`lili_validators.py:487-497`，CLAUDE.md
  Phase 8 记录过）形成明确不对称：SPEC 阶段专门加了"标签缺失就退化为全文搜索"的兜底，
  SCOUT 阶段从未补上同一课。且 `_call_qwen_search` 的重试逻辑（`lili_llm.py:106-127`，
  3 次指数退避）只覆盖"响应为空"这一种失败，"响应非空但解析不出关键字段"完全绕开重试，
  直接进 `save_rest_day`。这两次事件也完全绕开了 F-024/F-025 的账单故障分类器——
  `classify_scout_failure()` 只在 `scout_content` 为空的分支里被调用（`lili_pipeline.py:890`），
  非空但解析失败这条路径根本不会问"这是不是账单问题"，所以即便根因是账单相关的截断，
  也不会被现有的账单可见性机制捕捉到。
- **证据**：`01_Work_Log/2026-08-13-Diary.md`、`01_Work_Log/2026-08-15-Diary.md`、
  `01_Work_Log/2026-08-21-Diary.md` 三份技术备注逐字相同；`lili_pipeline.py:899-903`、
  `lili_validators.py:451-484`、`lili_llm.py:97-127` 代码走读确认上述路径与结构性缺口。
  08-21 这次发生在 F-033（08-22 落地，修的是 DeepSeek 兜底的"空响应不重试"）之前，且
  是 Qwen 自己返回了非空内容后解析失败——不在 F-033 修的那条路径上，两者是同一周里
  两个独立但外观相似（都以 rest day 收场）的故障模式，不要混为一谈。原始 `scout_content`
  （Qwen 的实际回复文本）没有被
  持久化在日记、日志或账本的任何字段里，所以本条目**只能确认"结构上没有兜底/没有重试"
  这个事实，无法确认两次的具体触发原因**（`max_tokens=4096` 截断是一个合理猜测——
  SCOUT prompt 要求的字段多、web search 结果可能被塞进模型的输出前段挤占尾部标签——
  但没有原始响应可查证，只是候选解释之一，不作为结论）。
- **应对**：本条目截至本次文档刷新（2026-08-23）仍未改代码——F-030 结尾定的"暂停新增
  机制，先看至少一周真实台账"的观察期，从 F-030 所在周（08-03~08-09）算起已经过去两周，
  本条目升级为"确认"后，候选修复（给 `parse_scout_response` 补 SPEC 同款兜底解析、把
  非空-但-不完整也纳入重试、在 `save_rest_day` 前落一份原始响应用于事后排查）**已不再
  受该暂停期约束，可以在下一次允许改动机制的会话中直接动手**，不必再等；本次文档刷新
  按既定范围只记录不改代码。
- **推论（待验证）**：结构性复现模式已确认，剩余待验证的是根因（截断 vs 格式漂移）——
  修复后应观察"Phase 1 (Scout) returned incomplete response" 是否消失或显著减少；如果
  修复后依然以相近频率出现，说明根因不在解析兜底/重试层面，需要重新诊断。

## F-032 · 自纠正内循环漏了 validate_tool() 的 USER_INPUT 双模式结构检查

- **日期**：2026-08-18 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构修复，不涉及具体模型能力发现
- **声明**：2026-08-18 真实生产事故——SPEC 一次通过（"从粘贴的邮件线程提取发票明细"，
  Format A / Mode 1），BUILD 写出的代码却完全没有引用 `USER_INPUT` 这个注入的全局变量，
  日志里没有任何"🔧 Self-correction round"记录，说明自纠正内循环把这份代码当成"干净"
  直接放行了，直到 `validate_tool()` 里一道独立的结构检查（直接搜源码字符串里有没有
  "USER_INPUT"）才把它拦下。根因：`run_tool_output()` 是把 `USER_INPUT` 当**真实全局
  变量**注入执行环境的，代码完全可以硬编码输出、彻底忽略这个变量，只要凑巧包含
  `MUST_CONTAIN` 要求的字符串，就能顺利通过自纠正内循环基于"跑一遍看输出对不对"的检查——
  这道检查从来没验证过"代码是不是真的读了输入"，只验证了"输出长得对不对"。这是跟
  F-021（Mode 3 浏览器检测缺失）完全同一类缺口：内循环的检查覆盖面没有跟上外层完整
  验证链，问题只能等跑到昂贵的那一步才被发现。
- **应对**：新增共享辅助函数 `_missing_user_input_pattern()`，逐字复用 `validate_tool()`
  自己的判定逻辑（源码里搜不到 `"globals().get('USER_INPUT'"` 也搜不到裸的
  `"USER_INPUT"` 才判定缺失）。接入两处：
  1. `self_correct_code`（固定轮次版）：语法检查通过后、真实执行之前先做这道结构检查——
     不需要跑一次子进程，比后面的执行检查更便宜。
  2. `agentic_self_correct_code`（工具调用版）：接进 `_execute_candidate()`，`run_and_check`
     和 `submit_final_code` 两条路径共用同一个检查点，模型不管走哪条路都躲不掉。
- **证据**：`tests/test_self_correct.py` 新增 `SelfCorrectUserInputPatternTests`（3 个
  测试：缺失时触发补丁、即使硬编码输出恰好满足 MUST_CONTAIN 也依然被拦、正常代码不会
  被误伤）；`tests/test_agentic_self_correct.py` 新增 `AgenticUserInputPatternTests`
  （3 个测试：`submit_final_code` 路径被拒、`run_and_check` 路径被拒、正常代码通过）。
  186 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果设计成立，"代码看起来干净、执行也不报错，却在 `validate_tool()`
  最后一刻因为结构检查被拒"这类失败应该在自纠正阶段就被拦下，不再消耗到外层验证链。
  这也提示一个通用规律，值得在后续每次给 `validate_tool()` 加新检查项时留意：**任何加进
  外层验证链的新检查，都该同时问一句"内循环是不是也该照着查一遍"**，否则同样的滞后
  发现模式会在下一个新检查项上重演。

## F-033 · 借鉴 deepseek-harness 的"能力接口"模式，统一 provider 重试逻辑

- **日期**：2026-08-20 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：纯 harness 结构修复，不涉及具体模型能力发现
- **声明**：研究 `deepseek-ai/deepseek-harness`（官方开源 agent harness）的 LLM adapter
  写法规范，确认了它的核心模式——每个 provider 实现同一个接口，注册后走统一路径，
  不是每个调用点各自手写。拿这个去对照现有代码，发现一个直接对应的真实 bug：千问的
  SCOUT 搜索一直有 3 次指数退避重试（`_call_qwen_search`），但 DeepSeek 作为 SCOUT
  兜底路径时是**单次裸调用，完全没有重试**——这正是 2026-08-19/20 两次休息日的根因之
  一：DeepSeek 返回空响应（项目自己的 FINDINGS F-003 早就确认过这是已知的瞬时性行为，
  应该重试），但兜底路径试一次就放弃了，跟千问自己的搜索待遇完全不对等。
- **应对**：新增共享重试原语 `call_with_retry()`（`lili_llm.py`）——3 次指数退避、
  空响应视同失败重试、可选 `on_error` 回调记录最后一次错误文本，逻辑跟千问搜索原来的
  手写循环完全等价，只是抽成了任何 provider 调用都能复用的公共函数。
  - `_call_qwen_search` 重构为基于这个原语（去重复，行为不变，全部测试原样通过验证）
  - 新增 `call_deepseek_scout_fallback()`，同样基于这个原语——DeepSeek 做 SCOUT 兜底
    现在也有了跟千问对等的 3 次重试，不再是"空一次就认输"
  - `lili_pipeline.py` 的 Phase 1 兜底逻辑从原来的内联 try/except 改成直接调用这个
    新函数，去掉了原来手写在 `evolve()` 里的那一段
- **证据**：`tests/test_llm.py` 新增 `CallWithRetryTests`（5 个测试：首次成功、空响应
  重试后成功、异常重试后成功、耗尽重试返回 None、`on_error` 回调收到错误文本）和
  `DeepSeekScoutFallbackTests`（4 个测试：空响应重试后成功、耗尽重试返回 None、无
  client 返回 None、错误文本被记录供后续账单故障分类使用）。195 个测试全绿，本地 3.13
  与真实 3.11 venv 双重验证，且验证了重构 `_call_qwen_search` 后原有测试行为不变。
- **推论（待验证）**：如果设计成立，未来"DeepSeek SCOUT 兜底空响应"类失败应该有更高
  概率在重试中自愈，不必等到千问也故障、DeepSeek 兜底又恰好一次性失败这种双重不巧合
  才会真正休息。这也是一个可以泛化的经验：**任何"该 provider 有重试、这个 provider
  没有"的不对称，都值得怀疑是历史遗留而非有意设计**——这次是从外部开源项目的写法规范
  反向照出了自己代码里的这处不对称，值得作为一种常规排查手段记下来。

## F-034 · 质量账本写入函数是 UTC/Beijing 日期错位 bug 家族的第三个未修实例

- **日期**：2026-08-23 发现（读代码机械确认，无需等待特定生产事件复现） · **状态**：
  确认（机制本身通过读代码 100% 确认；对已写入台账的历史条目造成了多大实际影响未逐条
  核实，见下）
- **模型**：纯 harness 结构问题，不涉及具体模型能力发现
- **声明**：CLAUDE.md Pitfalls 已记录两起同类事故并在 2026-08-10（`58a7e35`、`6a781e4`）
  修复：`weekly_evolution()` 和 `evolve()`（写日记/commit message/节流判断）过去都用
  `datetime.utcnow()` 而不是 Beijing 日期，导致跨凌晨的主时段运行（Beijing 02:00 =
  UTC 前一天 18:00）把内容写到错误日期的文件里，一次甚至覆盖了 2026-08-09 的周进化
  内容。修复方式是让这两个函数都改读 workflow 传入的 `LILI_DATE`（Beijing 日期）而不是
  裸 UTC。这次刷新文档时读 `src/lili_validators.py:670-691` 的 `_append_quality_ledger()`
  （质量账本 `tool_quality_ledger.jsonl` 的唯一写入点，`git log -S` 确认自
  `a7d05a2`（Phase 9 模块拆分）引入以来从未改过）发现它写入的 `"date"` 字段
  （`lili_validators.py:679`）依然是裸 `datetime.utcnow().strftime("%Y-%m-%d")`，
  完全没有读 `LILI_DATE`——是同一 bug 家族里被漏掉的第三处，且是三处里**唯一一处至今
  未修**的。触发条件与前两处完全相同：任何在 Beijing 00:00-08:00（即 UTC 前一天
  16:00-24:00）之间真正执行 BUILD+EVALUATE 阶段的运行，写入台账的日期会比日记/commit
  的 Beijing 日期晚一天记成前一天。当前主时段正是 Beijing 02:00（`.github/workflows/
  lili_daily.yml` 的 `0 18 * * *`），完全落在这个触发窗口里，不是罕见边界情况。
- **证据**：`git log --follow -S"_append_quality_ledger" -- src/lili_validators.py`
  显示引入于 `a7d05a2`，早于 `LILI_DATE` 机制本身（2026-08-10 才出现）；`lili_pipeline.py:855`
  与 `super_lili_weekly_evolution.py:1054` 两处都写成
  `os.environ.get("LILI_DATE") or datetime.utcnow()...`，`lili_validators.py:679` 没有
  同款写法，三处对比一目了然。台账里有一条间接线索与此一致但不作为确凿证明：
  `tool_quality_ledger.jsonl` 中日期为 `2026-08-17` 的条目（一次 shift-aware deadline
  换算工具的失败尝试，`passed: false`，engineering 3 / warmth 2）与当天日记
  `01_Work_Log/2026-08-17-Diary.md`（记录的是账单欠费导致 SCOUT 第一步就被拒、全程未
  跑到 BUILD）内容对不上，且概念上更接近 08-21/08-22 才正式成型的 deadline 工具——
  不能排除"同一 Beijing 日内多次独立尝试、最终以账单故障收尾"这类不涉及日期 bug 的
  解释，所以这一条只列为观察到的疑似现象，不算作确认本 bug 影响了具体哪一条历史记录，
  确认的只是代码本身缺这道防护。
- **应对**：本条目只记录观察，不改代码——本次是文档刷新范围，且 `lili_ledger_report.py`
  按 ISO 周聚合读的就是这个可能错位的 `date` 字段，贸然现在改会让本周日期和上周日期的
  分界本身发生变化，需要与下一次允许动代码的会话一起处理，同时决定是否需要为已写入的
  历史条目做一次性日期修正。
- **推论（待验证）**：如果修复后追踪几周，`lili_ledger_report.py` 输出的"按周通过率"
  在 Beijing 周日午夜前后的条目分界应该更稳定（不再出现台账日期和日记日期对不上的
  情况）；如果只是这一处的问题不大（多数运行落在不跨天的时段），修复前后周报数字应该
  几乎不变——那本条目的价值就主要是"防止未来在跨天时段调整 workflow 时踩同一个坑"，
  而不是"纠正了一个已经实质性扭曲历史统计的错误"。两种结果都值得记录，不预设答案。

## F-035 · 每周自进化连续两周被截断，`EVOLUTION_KNOBS` 静默丢失，函数名也修正为诚实命名

- **日期**：2026-08-27 · **状态**：观察中（机制已实测验证，尚无生产运行数据）
- **模型**：`deepseek-v4-pro`（自进化分析模型；截断本身是输出长度/预算问题，不是模型
  能力边界发现）
- **声明**：例行质量复盘时发现 `03_Evolution_Log/2026-08-16_Weekly_Evolution.md` 和
  `2026-08-23_Weekly_Evolution.md` 都在写到中途戛然而止——16 号那份停在"Letter to Next
  Week's Lili"之前，23 号那份更早，停在"Blindspot Analysis"中间一句话断掉。自进化的
  输出格式要求约 14 个依次排列的标签区块（反思→盲点→优势→待改进→开源工具→进化人格→
  技能列表→进化笔记→工程教训→给下周的信→日记→领域扩展→来源提案→**旋钮提案**），
  `call_gemini()`（旧名，见下）的 `max_tokens=8192` 明显不够用，模型写到一半预算耗尽，
  后面的区块全部丢失——而且旧的重试逻辑只在**报错**或**完全空白**时才重试，写了一半
  但非空的截断响应会被当成"成功"直接采信。直接后果：`lili_evolution_knobs.json` 的
  `week_start` 停留在 2026-08-03 不动——过去两周自进化提出的旋钮提案从未真正生成过，
  F-020 的门槛和 F-029 的事后复查这两周形同虚设，不是没生效，是根本没有新东西可以生效。
  排查过程中 owner 还指出一个命名问题：这个函数叫 `call_gemini`，但函数体和 docstring
  早就写明是纯 DeepSeek 实现（"Gemini removed"）——2026-07-03 那次把 Gemini 全部换成
  DeepSeek 时，`lili_pipeline.py` 里的同名函数改了名，这个漏改，只改了实现，留了个
  挂羊头卖狗肉的旧名字四十多天没人发现。
- **应对**：
  1. `max_tokens` 从 8192 提到 16384。
  2. 新增 `_is_truncated_evolution_response()`——检查响应里有没有出现最后的
     `---END---` 标签，没出现就判定截断。接入重试循环：截断时跟报错/空白一样触发重试
     （最多 3 次，指数退避），不再把"写了一半但非空"误判成"成功"。
  3. 3 次重试仍然截断时，**返回最后一次的部分内容而不是 None**——保留已有的"至少保存
     反思/盲点分析这些真实写出来的部分"这个行为，不因为旋钮没写出来就把整份反思都
     扔掉。
  4. `call_gemini` 重命名为 `call_deepseek_evolution`，纯改名不改行为。
- **证据**：`tests/test_evolution_truncation.py`（8 个测试：完整响应/截断响应的判定
  正确、截断时触发重试并在重试后成功接受完整响应、三次全部截断时回退到最后一次部分
  内容而不是彻底丢弃、报错和截断两种失败模式都能正确重试恢复、彻底失败时返回 None）。
  203 个测试全绿，本地 3.13 与真实 3.11 venv 双重验证。
- **推论（待验证）**：如果修复有效，下一次自进化应该能看到 `lili_evolution_knobs.json`
  的 `week_start` 真正刷新到最新一周，F-020/F-029 这两个机制重新开始有实际输入可以
  处理。这也是本文件里少有的"由 owner 而非机械核验发现的问题"——owner 追问"为什么
  还有 Gemini"这一句直接问出了命名债，值得记一笔：**偶尔让人看一眼代码里的具体名字/
  字符串，比任何自动化检查都更容易抓到这类"实现改了、名字没跟着改"的低概率但真实存在
  的疏漏**。

## F-036 · 每周自进化"连续两天重复运行"以新根因再次发生——backup 时段延迟跨过北京午夜，绕过了 F-020 时代已修的 LILI_DATE 机制

- **日期**：2026-08-30 ~ 08-31（本次文档刷新发现）· **状态**：确认（机制通过 cron 配置 +
  两次真实运行的 commit 时间戳 + 报告内容对比三方交叉验证）
- **模型**：纯 harness/GitHub Actions 调度问题，不涉及具体模型能力
- **声明**：2026-08-10（`58a7e35`）修复的是"skip 判断读 Beijing 日期、报告文件名却写
  UTC 日期"这一种错位；本次是同一症状（一周内两份几乎重叠的周进化报告）但**根因完全
  不同**——`LILI_DATE` 机制本身工作正常，两次运行各自内部的日期变量是自洽的，问题出在
  `lili_weekly_evolution.yml` 的三个 cron 时段里，20:05 北京（backup，对应 UTC 周日
  12:05）那一档在真实执行时被 GitHub Actions 排队延迟了超过 4.5 小时，实际跑到 UTC
  16:40，此时换算成北京时间已经是次日 00:40——`Check` 步骤当场用 `TZ=Asia/Shanghai
  date` 算出的 `TODAY` 变成了"08-31"而不是"08-30"，`03_Evolution_Log/2026-08-31_Weekly_
  Evolution.md` 当然不存在，skip 判断放行，于是同一周被从头到尾分析了两遍。
- **证据**：`git log` 显示 `cb60884`（🌸 2026-08-30 Weekly Evolution）commit 时间戳为
  `2026-08-30T01:16:27Z`（对应北京 08-30 09:16，即主时段延迟后的正常触发），`2554fe9`
  （🌸 2026-08-31 Weekly Evolution）commit 时间戳为 `2026-08-30T16:40:59Z`（对应北京
  08-31 00:40，backup 时段延迟超 4.5 小时后跨过北京午夜触发）；两份报告标题分别为
  "Weekly Evolution — 2026-08-24 → 2026-08-30" 与 "Weekly Evolution — 2026-08-25 →
  2026-08-31"，Reflection/Blindspot Analysis 两节讨论的是同一批四个工具（Decision
  Logic Extractor、Critique Memory Keeper、Easing Curve Rosetta、Invoice Follow-Up
  Sequencer），用词高度重叠（如两份都写"extract-then-summarize"是默认模式、都点名
  同一个 GitHub issue #5），确认是同一周被重复处理而非两次独立进化。
- **应对**：本次文档刷新范围内不改代码，仅记录根因供下一次允许动代码的会话处理。
  可能的修复方向：`Check` 步骤的 skip 判断不应只查"今天"这一个文件名，还应查询
  `03_Evolution_Log/` 里最新报告的实际覆盖周期（或干脆检查过去 6 天内是否已有任何
  报告），避免依赖"cron 触发时刻换算出的日期"这个在长延迟下不稳定的量。
- **推论**：`LILI_DATE` 类"把两处日期计算锚定到同一个字符串"的修复能解决"两个步骤
  在同一次运行里互相矛盾"，但解决不了"同一个 workflow 的两次独立触发，因为调度延迟
  跨越了午夜边界，各自内部都自洽但彼此不一致"——这是一类不同的竞态，任何用"今天的
  日期"做幂等判断的定时任务都有这个隐患，主时段和 backup 时段之间的时间差越大，
  跨午夜风险越高。

## F-037 · `EVOLUTION_KNOBS` 截断修复（F-035）落地后，`week_start` 仍连续第三、四周停留在 2026-08-03——修复是否生效目前从仓库状态无法判断

- **日期**：2026-09-02（本次文档刷新发现）· **状态**：观察中（有明确的滞留现象，但
  无法确认是修复未生效、还是回测门槛正确拒绝了本周的旋钮提案——两者从仓库状态看
  完全无法区分）
- **模型**：`deepseek-v4-pro`（自进化分析），涉及的是 harness 可观测性问题而非模型
  能力发现
- **声明**：`lili_evolution_knobs.json` 的 `week_start` 字段自 2026-08-09（`6fb0e58`/
  `2168aa8`，即 F-035 提到的截断问题发生之前）起从未再变化过，即便 F-035 的截断修复
  已于 2026-08-27（`b625e12`）上线、此后又跑过两次真实的周进化（2026-08-30、08-31，
  见 F-036），`week_start` 依然是 "2026-08-03"。这意味着要么截断修复没有真正解决问题
  （比如模型仍在别的位置提前中断、或 JSON 解析仍然失败），要么 `gate_evolution_
  proposal()`（F-020 的回测门槛）这两周都合理地拒绝了模型提出的旋钮提案，属于设计
  内的正常行为。`save_evolution_knobs_gated()` 把区分这两者的唯一线索——"(no knobs
  proposed this week)" / "malformed JSON, skipped" / "GATE REFUSED (test suite not
  green)" 这三种不同结果——只 `print()` 到 GitHub Actions 的 stdout，从未写入
  `03_Evolution_Log/`、日记或任何仓库内文件，导致这次文档刷新完全无法从 git 历史
  判断本周到底发生了三种情况里的哪一种。
- **证据**：`git log --oneline -- lili_evolution_knobs.json` 只有两条记录（均为
  2026-08-09），此后包括本周 08-30/08-31 在内的所有周进化 commit 都不触碰这个文件；
  `src/super_lili_weekly_evolution.py:1019-1054`（`save_evolution_knobs_gated`）三条
  `return` 分支（空提案 / JSON 解析失败 / 门槛拒绝）都只返回字符串给
  `knobs_summary`，该变量在 `weekly_evolution()`（同文件 1142 行）里只经过
  `print(f"  {knobs_summary}")`，未见任何写文件调用。
- **应对**：本次不改代码。建议下一次允许动代码的会话把 `knobs_summary`（以及
  F-029 `run_retrospective_review` 的 `retrospective_summary`）持久化进
  `03_Evolution_Log/{date}_Weekly_Evolution.md` 或单独的旋钮变更日志文件，这样
  "本周为什么没有新旋钮"本身也能像 F-024 的账单故障一样在仓库里可见，而不必依赖
  翻 GitHub Actions 的运行日志。
- **推论（待验证）**：一旦诊断信息落盘，下次刷新就能直接看出连续停滞的原因——
  如果是回测门槛持续正确拒绝提案，说明 F-020 在按设计工作，`week_start` 长期不动
  反而是健康信号；如果是解析/截断链路仍有问题，则 F-035 的修复需要返工。目前两种
  可能性都要保留，不预设哪一种更可能。

## F-038 · Qwen 侧的 401 "invalid_api_key" 被泛化的"账单故障"文案吞掉，休息日诊断给出的处理建议对 Qwen 是错的

- **日期**：2026-08-31 ~ 09-02，连续三天（本次文档刷新发现，09-02 当天仍在持续）·
  **状态**：确认（三次独立同构事件 + 代码机械核验）
- **模型**：纯 harness 错误分类问题，不涉及具体模型能力；受影响的两个 provider 是
  `qwen-plus`（SCOUT 搜索）与 DeepSeek 系列（fallback/BUILD 等）
- **声明**：F-024/F-025 引入的 `is_billing_error()` + `classify_scout_failure()`
  把"账单/欠费"类错误统一渲染成"INFRASTRUCTURE OUTAGE...requires a human to top up
  billing"的横幅，这对 DeepSeek 这三天返回的 `402 Insufficient Balance` 是准确的，
  但 Qwen 这三天返回的其实是 `401 Incorrect API key provided`（`code:
  invalid_api_key`）——这是凭证失效/错误，不是余额问题，充值账单**不会**修好 Qwen
  这一侧。因为 `classify_scout_failure` 用的是 `is_billing_error(qwen) or
  is_billing_error(deepseek)`，只要有一个 provider 命中账单特征词就整体判定为账单
  故障并写入统一的"去充值"文案，日记模板最后一行"账充上以后，明天流水线应该会自动
  恢复正常"对 Qwen 的实际问题是误导性建议——即便原始错误文本被逐字附在日记里
  （所以信息没有丢失），面向人类的结论句仍然只讲了一半真相，而这一半人类更可能先看到。
- **证据**：`01_Work_Log/2026-08-31-Diary.md`、`2026-09-01-Diary.md`、
  `2026-09-02-Diary.md` 三份"技术细节"完全同构：Qwen 均为
  `Error code: 401 - ... 'code': 'invalid_api_key'`，DeepSeek 均为
  `Error code: 402 - ... 'Insufficient Balance'`；`src/lili_llm.py:40-48`
  的 `_BILLING_ERROR_SIGNATURES` 元组不含 "invalid_api_key"/"incorrect api key"
  等凭证类关键词，`is_billing_error("Incorrect API key provided...")` 对 Qwen
  错误单独判断会返回 `False`；`src/lili_pipeline.py:627-637` 的
  `classify_scout_failure()` 用 `or` 而非分别处理两个 provider，一旦 DeepSeek 命中
  账单特征就统一套用账单话术，不区分另一个 provider 的真实故障类型。
- **应对**：本次不改代码（文档刷新范围）。建议下次迭代把 `classify_scout_failure`
  改成分别报告每个 provider 的故障类别（账单 / 凭证 / 未知），而不是任一方命中账单
  特征词就整体归类；诊断文案应分别给出"充值"和"检查/轮换 API Key"两种不同的下一步
  动作。**此外这是一个需要 owner 立即处理的真实运营问题**：截至本次刷新（09-02），
  Qwen API Key 已经连续三天返回"无效"，充值 DeepSeek 账户并不能让 SCOUT 恢复正常，
  需要 owner 单独检查/更新 `QWEN_API_KEY` 这个 GitHub Secret。
- **推论**：F-024/F-025 解决的是"账单故障看起来像创作性休息日"这个可见性问题，但
  没有覆盖"两个 provider 同时故障、但故障类型不同"这种组合情况——错误分类的粒度
  目前是"是否命中账单关键词"的布尔值，一旦有非账单类故障（凭证、限流、模型下线等）
  与账单类故障同时出现，前者会被后者的诊断文案盖住。这提示 F-024 类"让故障可见"的
  修复本身也需要定期检查其分类粒度是否还够用，而不是修一次就当作永久解决。

## F-039 · 测试套件当前是红的：一个硬编码日期的定时炸弹测试到期失效，而自进化每周写 `src/**` 的 commit 从未真正触发过 CI

- **日期**：2026-09-02（本次文档刷新发现）· **状态**：确认（本地 + 真实 CI 运行记录双重
  核验，非模型能力问题，纯 harness/CI 配置问题）
- **模型**：无关模型能力
- **声明**：两个独立问题叠加，导致测试套件从大约 2026-08-29/30 起处于红色但无人发现：
  (1) `tests/test_prompts.py::test_daily_offender_concepts_injected_when_present`
  （`da5f250`，2026-08-03/04 引入，F-021 的一部分）构造了一条 `"date": "2026-08-01"`
  的假账本记录，断言它会被 `lili_ledger_report.load_entries(days=28)` 判定为
  28 天内的"repeat offender"并注入 SCOUT 提示词——但 `load_entries()`
  （`src/lili_ledger_report.py:42`）用的是真实墙钟 `datetime.now(timezone.utc)` 而非
  可注入/可冻结的时间，测试从未冻结"今天"。2026-08-01 加 28 天等于 2026-08-29，
  真实日历一旦越过这个点，这条固定日期的 fixture 就必然被过滤出 28 天窗口，测试从
  "必然通过"变成"必然失败"，与代码是否正确无关，纯粹是挂钟时间的定时炸弹。
  (2) 这次失效之所以三天多都没人发现，是因为承载它的这部分代码
  （`src/lili_engineering.py`、`src/lili_soul.py`、`src/lili_blindspot.py`，均属于
  `lili_tests.yml` 监听的 `src/**` 路径）的最近两次改动，都来自每周自进化工作流自己
  用默认 `GITHUB_TOKEN` 发起的 `git push`（`cb60884`、`2554fe9`，作者
  "Super-Lili (Evolution)"）——GitHub Actions 的既定规则是"用 `GITHUB_TOKEN` 发起的
  push 不会触发其他以 `on: push` 监听的 workflow"（防止 workflow 互相触发造成死循环），
  所以 `lili_tests.yml` 对这两次 push 根本没有运行过，红色测试无声无息地留在
  `main` 分支上。对照同一时期由人类/Claude 会话用个人身份 push 的 `b625e12`
  （同样改了 `src/**`），CI 正常触发并跑绿——两者路径完全相同，唯一区别是推送者
  的认证身份，直接印证了这条 GitHub 平台规则在本仓库的真实效果。
- **证据**：本地 `python3 -m unittest discover -s tests` 于 2026-09-02 运行结果为
  `FAILED (failures=1, skipped=1)`，唯一失败即上述测试，`AssertionError:
  'Phone Screenshot Organizer' not found in ...`；`gh run list --workflow=lili_tests.yml`
  显示最近一次真正跑过的记录是 `b625e12`（2026-08-27，success），此后
  `cb60884`（2026-08-30）与 `2554fe9`（2026-08-31）两次 push 完全没有对应的
  workflow run 记录；`git log --author="Super-Lili (Evolution)"` 交叉核对显示，
  几乎每一周的自进化 commit 都会改到 `lili_soul.py`/`lili_engineering.py`/
  `lili_blindspot.py`，也就是说这个 CI 盲区并非本周新出现，而是自 Phase 10
  自进化开始写这几个文件以来（2026-07-17 前后）就一直存在，只是过去没有一个
  恰好在这个盲区窗口期内失效的测试，这次是第一次真正暴露出来。
- **应对**：本次为文档刷新范围，不改代码。建议下一次允许改代码的会话：
  ① 修 `test_daily_offender_concepts_injected_when_present` 本身——用可控的
  相对日期（比如基于 `datetime.now()` 动态计算一个"27 天前"的 fixture 日期，或者
  给 `load_entries` 增加一个可注入的"当前日期"参数用于测试）而不是写死
  `"2026-08-01"`；② 更根本地，审查测试套件里其他用绝对日期字符串构造 fixture
  的用例，判断还有没有同类定时炸弹；③ 单独给自进化的 `git push` 配一个不是默认
  `GITHUB_TOKEN` 的凭据（例如一个专用 PAT，类似 Phase 12 提到过、后来疑似停用的
  `lili-deploy`），或者在 `lili_weekly_evolution.yml` 里增加一步在 push 后
  手动 `workflow_dispatch` 触发 `lili_tests.yml`，让自进化自己修改的代码也能被
  它本该受到的测试网接住。
- **推论**："CI 会在每次 push 时兜底"这个假设，对由自动化系统自己用默认凭据推送的
  commit 并不成立——这类系统如果会修改自己的源码（本项目的每周自进化正是如此），
  它对自己代码的测试覆盖存在一个结构性盲区，而且这个盲区不会主动报错，只会在某个
  依赖真实时间的脆弱测试恰好失效时才被动暴露。这和 F-013 的自写护栏（`_guarded_write`
  在写入前用 `ast.parse()`+`exec()` 自检）是同一类问题的不同侧面：自我修改的安全网
  不能假设外部 CI 会兜底，因为外部 CI 可能根本没有被触发。

---

*新发现的写入规则：编号递增，不删除旧条目；**必须写明具体模型与版本**（无模型归属的
"AI 能力"结论无法检验）；被推翻的发现改状态为"已推翻"并注明证据（含推翻它的模型）——
推翻记录与确认记录同等珍贵，模型升级推翻旧边界本身就是最有价值的发现。*
