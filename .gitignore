__pycache__/
*.pyc
*.pyo
.DS_Store
.env

# Tool demo output — generated at runtime, not part of the repo
demo_*/
*.csv
*.txt
!requirements.txt

# Generated HTML files from local tool runs — not part of the repo
*_clock.html
*_weave.html
*_bloom.html
*_compass.html
*_catalyst.html

# Any HTML file landing directly in repo root is a stray tool-run artifact
# (real site HTML lives under docs/ and 02_Toolbox/, never at root)
/*.html
# ...except the hand-maintained architecture diagram - Super-Lili is a fully
# open self-evolution experiment, so its architecture is public.
!/super_lili_workflow.html

# Claude Code internals — never commit these
.claude/
.clone/

# Environment variables — never commit
.env
