name: 🌸 Super-Lili Daily Adventure

on:
  schedule:
    - cron: '30 0 * * *'   # 08:30 Beijing (primary) — 30min after quota reset
    - cron: '30 6 * * *'   # 14:30 Beijing (backup)
    - cron: '30 12 * * *'  # 20:30 Beijing (backup)
  workflow_dispatch:

jobs:
  forge:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check
        id: check
        run: |
          DATE=$(TZ='Asia/Shanghai' date +'%Y-%m-%d')
          DOW=$(TZ='Asia/Shanghai' date +'%u')
          if [ "$DOW" = "7" ] || [ -f "01_Work_Log/${DATE}-Diary.md" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Run
        if: steps.check.outputs.skip == 'false'
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONPATH: src
        run: python src/super_lili_brain.py

      - name: Commit
        if: steps.check.outputs.skip == 'false'
        run: |
          git config user.name "Super-Lili (Bot)"
          git config user.email "lili-bot@users.noreply.github.com"
          git add .
          git diff --cached --quiet || git commit -m "✨ $(TZ='Asia/Shanghai' date +'%Y-%m-%d') Daily Adventure"
          git pull --rebase origin main
          git push
