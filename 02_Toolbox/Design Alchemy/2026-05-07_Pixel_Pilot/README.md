# 🛠️ Skill: Pixel Pilot

> The Infinite Loading Loop of Lost Pixels

### Function
A Python script to diagnose common client-side issues with image files (size, format) before attempting a web upload.

### Usage
Run `python main.py`.